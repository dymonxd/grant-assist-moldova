---
phase: 01-foundation-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/loading.tsx
  - src/app/error.tsx
  - src/app/not-found.tsx
  - next.config.ts
  - package.json
  - tsconfig.json
  - .env.local.example
  - supabase/config.toml
  - supabase/migrations/001_initial_schema.sql
  - supabase/seed.sql
autonomous: true
requirements:
  - FNDTN-01
  - FNDTN-02
  - GEN-01
  - GEN-02
  - GEN-03

must_haves:
  truths:
    - "Next.js 16 app builds successfully with zero TypeScript errors"
    - "Browser shows Romanian UI shell with Geist fonts at localhost:3000"
    - "Layout is responsive at 375px mobile viewport (no horizontal scroll)"
    - "Loading spinner renders on navigation, error boundary catches thrown errors, 404 page shows Romanian message"
    - "Supabase migration creates all 11 tables with RLS enabled on every table"
    - "Unauthenticated query to company_profiles as anon role returns zero rows"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with html lang=ro, Geist fonts, Tailwind body classes"
      contains: "lang=\"ro\""
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS-first configuration with brand theme"
      contains: "@import \"tailwindcss\""
    - path: "src/app/page.tsx"
      provides: "Landing page shell with Romanian placeholder content"
      contains: "Descopera"
    - path: "src/app/loading.tsx"
      provides: "Global loading state"
      min_lines: 5
    - path: "src/app/error.tsx"
      provides: "Global error boundary"
      contains: "use client"
    - path: "src/app/not-found.tsx"
      provides: "404 page in Romanian"
      min_lines: 5
    - path: "supabase/migrations/001_initial_schema.sql"
      provides: "Complete schema with 11 tables, RLS, indexes, triggers, functions"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "next.config.ts"
      provides: "Next.js config with serverExternalPackages for react-pdf"
      contains: "serverExternalPackages"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals"
    - from: "src/app/layout.tsx"
      to: "geist"
      via: "font import"
      pattern: "GeistSans|geist/font"
    - from: "supabase/migrations/001_initial_schema.sql"
      to: "Supabase Postgres"
      via: "supabase db push or migration"
      pattern: "CREATE TABLE"
---

<objective>
Scaffold the Next.js 16 project with all dependencies, create the Supabase database schema with Row Level Security, and build the Romanian UI shell with mobile-first responsive layout and boundary states (loading, error, not-found).

Purpose: Everything else in the project depends on the scaffolded app and deployed schema. This is the root of the dependency tree.
Output: A buildable Next.js 16 app with Tailwind v4, shadcn/ui, Geist fonts, and a Supabase database with 11 tables, RLS policies, indexes, triggers, and functions.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-validation/01-RESEARCH.md
@plan.md (Section 5: Database Schema -- complete table definitions, indexes, RLS policies, functions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with all Phase 1 dependencies</name>
  <files>package.json, next.config.ts, tsconfig.json, src/app/globals.css, .env.local.example</files>
  <action>
1. Run `npx create-next-app@latest grants-moldova --tailwind --typescript --eslint --app --src-dir --turbopack` in the project root. If the current directory is already the project root (which it is -- `/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)`), run the command to create a subdirectory, then move all files up to the current directory, or run inside the current directory if create-next-app supports `.` as the name.

2. Install Phase 1 dependencies:
   ```bash
   npm install @supabase/supabase-js @supabase/ssr iron-session geist
   npm install @react-pdf/renderer jspdf
   ```

3. Initialize shadcn/ui:
   ```bash
   npx shadcn@latest init -d
   ```
   Accept defaults (New York style, zinc base, CSS variables enabled).

4. Initialize Supabase:
   ```bash
   npx supabase init
   ```

5. Update `next.config.ts` to include serverExternalPackages for @react-pdf/renderer:
   ```typescript
   import type { NextConfig } from 'next'
   const nextConfig: NextConfig = {
     serverExternalPackages: ['@react-pdf/renderer'],
   }
   export default nextConfig
   ```

6. Replace `src/app/globals.css` with Tailwind v4 CSS-first configuration:
   - Use `@import "tailwindcss"` (NOT @tailwind directives -- those are v3)
   - Define @theme block with: --font-sans (Geist Sans fallback chain), --font-mono (Geist Mono fallback chain), brand colors (--color-background: #fafafa, --color-foreground: #18181b, --color-accent: #e67e22, --color-accent-foreground: #ffffff, --color-muted: #71717a, --color-border: #e4e4e7)
   - Add @custom-variant dark block

7. Create `.env.local.example` with all required env vars (no real values):
   ```
   NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
   SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
   SESSION_SECRET=minimum_32_character_secret_here_change_me
   ```

IMPORTANT: Do NOT create a tailwind.config.js or tailwind.config.ts file. Tailwind v4 uses CSS-first configuration only. If shadcn init creates one, it may need to be reconciled with the CSS-first approach.
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Project builds with zero errors. package.json includes all Phase 1 deps. globals.css uses @import "tailwindcss" with @theme block. next.config.ts has serverExternalPackages. supabase/ directory initialized.</done>
</task>

<task type="auto">
  <name>Task 2: Create Romanian UI shell with Geist fonts and boundary states</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/app/loading.tsx, src/app/error.tsx, src/app/not-found.tsx</files>
  <action>
1. Update `src/app/layout.tsx`:
   - Import GeistSans and GeistMono from 'geist/font/sans' and 'geist/font/mono'
   - Set `<html lang="ro">` with className including both font variables
   - Set body className to "font-sans antialiased bg-background text-foreground min-h-screen"
   - Export metadata with title "GrantAssist Moldova" and description "Descopera granturile disponibile pentru afacerea ta"
   - Wrap children in a semantic layout: header (placeholder nav with "GrantAssist Moldova" text), main (children), footer (copyright)
   - Header should be responsive: flex with justify-between, px-4 py-3, max-w-5xl mx-auto
   - Footer: text-sm text-muted, centered, py-4

2. Update `src/app/page.tsx`:
   - Create a centered landing page shell (this is a placeholder -- Phase 2 builds the real landing page)
   - Center content vertically and horizontally using flex
   - Show headline: "Descopera granturile disponibile pentru afacerea ta" in text-3xl font-bold
   - Show subtitle: "Introdu IDNO-ul companiei sau descrie ideea ta de afacere" in text-muted
   - Add a placeholder input (disabled) and button to show the layout
   - Mobile-first: full width on mobile (px-4), max-w-lg on desktop
   - This is a SHELL only -- not the real landing page implementation

3. Create `src/app/loading.tsx`:
   - Simple centered loading spinner using Tailwind animate-spin on a circular div
   - Romanian text below: "Se incarca..."
   - Must be a Server Component (no "use client")

4. Create `src/app/error.tsx`:
   - Must have "use client" directive (Next.js requirement for error boundaries)
   - Accept error and reset props
   - Show Romanian error message: "Ceva nu a mers bine"
   - Show error.message in text-sm text-muted
   - Show "Incearca din nou" button that calls reset()
   - Centered layout, mobile-friendly padding

5. Create `src/app/not-found.tsx`:
   - Romanian 404 message: "Pagina nu a fost gasita"
   - Link back to home: "Inapoi la pagina principala"
   - Centered, simple layout
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 app files exist and build. Layout uses html lang="ro" with Geist font variables. Page shows Romanian placeholder content. Loading, error, and not-found states are implemented with Romanian text. Layout is mobile-first with max-width container.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy Supabase schema with 11 tables, RLS, indexes, triggers, and functions</name>
  <files>supabase/migrations/001_initial_schema.sql, supabase/seed.sql</files>
  <action>
Create `supabase/migrations/001_initial_schema.sql` with the complete schema from plan.md Section 5. This is a single migration file containing everything:

**11 Tables (in dependency order):**
1. `profiles` -- id UUID PK (= auth.users.id), name TEXT, phone TEXT, is_admin BOOLEAN DEFAULT false, email_notifications BOOLEAN DEFAULT true, created_at TIMESTAMPTZ DEFAULT now()
2. `company_profiles` -- id UUID PK DEFAULT gen_random_uuid(), user_id UUID REFERENCES profiles(id) NULLABLE, idno TEXT NULLABLE, business_idea TEXT NULLABLE, company_name TEXT, industry TEXT, location TEXT, legal_form TEXT, purchase_need TEXT, enriched_data JSONB, share_token UUID UNIQUE DEFAULT gen_random_uuid(), share_token_expires_at TIMESTAMPTZ DEFAULT now() + interval '30 days', created_at TIMESTAMPTZ DEFAULT now()
3. `grants` -- id UUID PK DEFAULT gen_random_uuid(), name TEXT NOT NULL, provider_agency TEXT NOT NULL, description TEXT, max_funding NUMERIC, currency TEXT DEFAULT 'MDL', deadline TIMESTAMPTZ, status TEXT CHECK (status IN ('draft', 'active', 'expired')) DEFAULT 'draft', source_type TEXT CHECK (source_type IN ('online_form', 'uploaded_template', 'manual')), eligibility_rules JSONB, scoring_rubric JSONB, required_documents JSONB, source_form_url TEXT, guidelines_pdf_path TEXT, form_template_path TEXT, excel_template_path TEXT, version INTEGER DEFAULT 1, last_scraped_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now()
4. `grant_application_fields` -- id UUID PK, grant_id UUID REFERENCES grants(id) ON DELETE CASCADE, field_order INTEGER NOT NULL, field_label TEXT NOT NULL, field_type TEXT DEFAULT 'textarea', is_required BOOLEAN DEFAULT true, character_limit INTEGER, helper_text TEXT
5. `saved_grants` -- id UUID PK, user_id UUID REFERENCES profiles(id) ON DELETE CASCADE, grant_id UUID REFERENCES grants(id) ON DELETE CASCADE, created_at, UNIQUE(user_id, grant_id)
6. `applications` -- id UUID PK, user_id UUID REFERENCES profiles(id) NULLABLE, grant_id UUID REFERENCES grants(id), company_profile_id UUID REFERENCES company_profiles(id) NULLABLE, status TEXT CHECK (status IN ('in_progress', 'completed', 'exported')) DEFAULT 'in_progress', field_snapshot JSONB, created_at, updated_at
7. `application_sections` -- id UUID PK, application_id UUID REFERENCES applications(id) ON DELETE CASCADE, grant_field_id UUID REFERENCES grant_application_fields(id), user_brief TEXT, ai_draft TEXT, final_text TEXT, is_saved BOOLEAN DEFAULT false, created_at, updated_at
8. `notifications_log` -- id UUID PK, user_id UUID REFERENCES profiles(id), application_id UUID REFERENCES applications(id) NULLABLE, grant_id UUID REFERENCES grants(id) NULLABLE, type TEXT CHECK (type IN ('deadline_reminder', 'abandoned_draft', 'grant_expiring', 'new_grant_match')), channel TEXT CHECK (channel IN ('email', 'sms')), sent_at TIMESTAMPTZ DEFAULT now()
9. `analytics_events` -- id UUID PK, session_id TEXT NOT NULL, user_id UUID REFERENCES profiles(id) NULLABLE, event_type TEXT NOT NULL, event_data JSONB, referrer_url TEXT, device_type TEXT, created_at TIMESTAMPTZ DEFAULT now()
10. `analytics_daily_summary` -- date DATE NOT NULL, stage TEXT NOT NULL, count INTEGER DEFAULT 0, device_type TEXT, top_referrers JSONB, PRIMARY KEY (date, stage, device_type)

That is 10 tables. The 11th table is `profiles` referenced above (total = profiles + company_profiles + grants + grant_application_fields + saved_grants + applications + application_sections + notifications_log + analytics_events + analytics_daily_summary = 10). On review, the plan says 11 tables. Check if there's a missed table. If only 10, note this -- the "11" count may include the Supabase auth.users table which is managed by Supabase automatically. Proceed with all 10 application tables.

**Indexes:**
- `CREATE UNIQUE INDEX idx_company_profiles_idno ON company_profiles (idno) WHERE idno IS NOT NULL;`
- `CREATE INDEX idx_analytics_events_created ON analytics_events (created_at);`
- `CREATE INDEX idx_applications_user ON applications (user_id);`
- `CREATE INDEX idx_applications_status ON applications (status);`
- `CREATE INDEX idx_grants_status_deadline ON grants (status, deadline);`

**RLS Policies (enable RLS on ALL tables):**
- `profiles`: authenticated users read/update own row (auth.uid() = id), admins read all
- `company_profiles`: authenticated users read/update own (auth.uid() = user_id), shared profiles readable via share_token with expiry check, NO anon INSERT/UPDATE/DELETE (use service role for anonymous profile CRUD)
- `grants`: anon+authenticated can read where status='active', admins can manage all
- `grant_application_fields`: anon+authenticated can read (public), admins can manage
- `saved_grants`: authenticated users read/insert/delete own (auth.uid() = user_id)
- `applications`: authenticated users read/update own (auth.uid() = user_id)
- `application_sections`: access via application ownership (join to applications where user_id = auth.uid())
- `notifications_log`: admin only (is_admin check)
- `analytics_events`: admin only (is_admin check)
- `analytics_daily_summary`: admin only (is_admin check)

**Trigger + Function:**
```sql
-- Auto-create profile on auth signup
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, name)
  VALUES (new.id, COALESCE(new.raw_user_meta_data->>'name', ''));
  RETURN new;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();
```

**Merge function:**
```sql
CREATE OR REPLACE FUNCTION public.claim_company_profile(p_profile_id UUID, p_user_id UUID)
RETURNS VOID AS $$
BEGIN
  UPDATE company_profiles
  SET user_id = p_user_id
  WHERE id = p_profile_id AND user_id IS NULL;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

**updated_at trigger for tables that have it:**
```sql
CREATE OR REPLACE FUNCTION public.update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Apply to grants, applications, application_sections
CREATE TRIGGER set_updated_at BEFORE UPDATE ON grants FOR EACH ROW EXECUTE FUNCTION update_updated_at();
CREATE TRIGGER set_updated_at BEFORE UPDATE ON applications FOR EACH ROW EXECUTE FUNCTION update_updated_at();
CREATE TRIGGER set_updated_at BEFORE UPDATE ON application_sections FOR EACH ROW EXECUTE FUNCTION update_updated_at();
```

**Seed data (`supabase/seed.sql`):**
Create 3 sample Moldovan grants with realistic data:
1. AIPA - Agricultural grant for equipment (active, deadline 2026-06-30, max 500000 MDL)
2. ODA - Digital transformation grant (active, deadline 2026-08-15, max 300000 MDL)
3. EU4Moldova - Green energy grant (draft, deadline 2026-09-01, max 750000 MDL)
Each grant should have 2-3 eligibility_rules in JSONB, a scoring_rubric with 3-4 criteria, and required_documents.
Also create 3-4 grant_application_fields per grant with realistic Romanian field labels (e.g., "Descrierea activitatii companiei", "Obiectivele proiectului", "Bugetul estimat").
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx next build 2>&1 | tail -5 && echo "---SCHEMA CHECK---" && grep -c "CREATE TABLE" supabase/migrations/001_initial_schema.sql && grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/001_initial_schema.sql && grep -c "CREATE POLICY" supabase/migrations/001_initial_schema.sql</automated>
  </verify>
  <done>Migration file exists with 10 application tables (11th is auth.users managed by Supabase). RLS enabled on all 10 tables. At least 10 RLS policies defined. 5 indexes created. handle_new_user trigger creates profile on signup. claim_company_profile function exists. update_updated_at trigger on 3 tables. Seed data has 3 grants with fields. Build still passes.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes with zero errors
2. `grep -c "CREATE TABLE" supabase/migrations/001_initial_schema.sql` returns 10
3. `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/001_initial_schema.sql` returns 10
4. `grep "lang=" src/app/layout.tsx` shows `lang="ro"`
5. All boundary state files exist: loading.tsx, error.tsx, not-found.tsx
6. `grep "@import" src/app/globals.css` shows `@import "tailwindcss"` (v4, not v3 @tailwind)
</verification>

<success_criteria>
- Next.js 16 app builds successfully with Tailwind v4, shadcn/ui, and Geist fonts
- Romanian UI shell renders with mobile-first responsive layout
- Loading, error, and 404 boundary states implemented in Romanian
- Supabase migration contains all tables, RLS, indexes, triggers, and functions
- Seed data provides realistic Moldovan grants for development
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-validation/01-01-SUMMARY.md`
</output>
