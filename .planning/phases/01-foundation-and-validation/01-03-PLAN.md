---
phase: 01-foundation-and-validation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/api/pdf/test/route.ts
autonomous: false
requirements:
  - FNDTN-07

must_haves:
  truths:
    - "PDF test route exists and handles both @react-pdf/renderer success and jspdf fallback"
    - "A decision is documented on which PDF strategy to use going forward"
  artifacts:
    - path: "src/app/api/pdf/test/route.ts"
      provides: "PDF generation validation endpoint"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/pdf/test/route.ts"
      to: "@react-pdf/renderer"
      via: "dynamic import with try/catch"
      pattern: "renderToBuffer"
---

<objective>
Create the PDF generation test route that validates @react-pdf/renderer on Vercel serverless, with Romanian diacritics test content. If @react-pdf/renderer fails, confirm jspdf as the fallback strategy. This answers the open question from research before Phase 5 depends on it.

Purpose: PDF generation is the highest-risk infrastructure decision in Phase 1. @react-pdf/renderer has documented compatibility issues with Next.js App Router on Vercel. Testing now prevents Phase 5 from hitting a wall.
Output: Working PDF test route and a documented decision on PDF strategy.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-validation/01-RESEARCH.md
@.planning/phases/01-foundation-and-validation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PDF generation test route with dual-strategy validation</name>
  <files>src/app/api/pdf/test/route.ts</files>
  <action>
Create `src/app/api/pdf/test/route.ts` that tests PDF generation with Romanian diacritics:

1. **Primary strategy -- @react-pdf/renderer (server-side):**
   - Use dynamic import: `const { renderToBuffer } = await import('@react-pdf/renderer')`
   - Import React via dynamic import
   - Import Document, Page, Text, StyleSheet from @react-pdf/renderer
   - Create a test document with:
     - A4 page with 30pt padding
     - Title text: "GrantAssist Moldova - Test PDF"
     - Romanian diacritics test line: "Caractere romanesti: ă, â, î, ș, ț, Ă, Â, Î, Ș, Ț"
     - A longer Romanian paragraph: "Aceasta este o aplicatie de test pentru generarea PDF-urilor cu diacritice romanesti corecte."
   - Call renderToBuffer() and return as Response with Content-Type application/pdf
   - Use createElement (not JSX) since this is a route handler, not a component file -- OR use .tsx extension and JSX

2. **Fallback -- jspdf (client-side documented):**
   - If @react-pdf/renderer throws any error, catch it
   - Return a JSON response (status 200, not 500) with:
     ```json
     {
       "primary_strategy": "react-pdf-failed",
       "error": "<error message>",
       "fallback_strategy": "jspdf-client-side",
       "recommendation": "Use jspdf with embedded Geist Sans TTF font on the client side",
       "next_steps": [
         "Download Geist Sans .ttf from npm package",
         "Use jspdf.addFont() with base64-encoded font",
         "Generate PDF client-side in the browser",
         "Romanian diacritics will render correctly with custom font"
       ],
       "phase5_impact": "Export button will trigger client-side PDF generation instead of server-side route"
     }
     ```

3. **Success response (if react-pdf works):**
   - Return the PDF buffer directly with headers:
     - Content-Type: application/pdf
     - Content-Disposition: inline; filename="test-diacritics.pdf"
   - The generated PDF should be viewable in-browser to visually confirm diacritics render

IMPORTANT: The route file can be .ts (using React.createElement) or .tsx (using JSX). Either works for route handlers. Use .ts with createElement to match the research pattern.
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx next build 2>&1 | tail -5 && echo "---ROUTE CHECK---" && grep -c "renderToBuffer" src/app/api/pdf/test/route.ts</automated>
  </verify>
  <done>PDF test route exists at /api/pdf/test, builds without errors, handles both success and failure cases. Route uses @react-pdf/renderer as primary with jspdf fallback documented in error response.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Vercel and verify PDF generation</name>
  <what-built>PDF test route that validates @react-pdf/renderer on Vercel serverless. Locally it may work, but the real test is on Vercel where bundling differences can cause failures.</what-built>
  <how-to-verify>
1. Deploy the app to Vercel: `npx vercel --yes` (or connect the repo to Vercel dashboard)
2. Visit `https://your-deployment-url/api/pdf/test` in a browser
3. **If you see a PDF displayed in-browser:**
   - Check that Romanian characters (ă, â, î, ș, ț) render correctly (not as boxes or question marks)
   - If diacritics look correct: type "react-pdf works"
   - If diacritics are broken: type "react-pdf diacritics broken"
4. **If you see a JSON response (fallback triggered):**
   - The JSON will explain the error and recommend jspdf
   - Type "jspdf fallback" and the error message shown
5. **If you see a 500 error page:**
   - Check Vercel function logs for the error
   - Type "vercel error" and paste the error
  </how-to-verify>
  <resume-signal>Type one of: "react-pdf works", "react-pdf diacritics broken", "jspdf fallback: [error]", or "vercel error: [details]"</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build` passes with PDF test route included
2. PDF test route handles both success and failure gracefully (no unhandled crashes)
3. Vercel deployment produces a definitive answer on PDF strategy
4. Decision is captured in SUMMARY.md for Phase 5 to reference
</verification>

<success_criteria>
- PDF test route exists and builds
- Route has been tested on actual Vercel deployment (not just local dev)
- A clear decision is documented: either "@react-pdf/renderer works on Vercel" or "use jspdf client-side as fallback"
- Phase 5 can reference this decision without re-investigating
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-validation/01-03-SUMMARY.md`
Include the PDF strategy decision prominently in the summary -- this is critical context for Phase 5.
</output>
