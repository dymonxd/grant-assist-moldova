---
phase: 02-data-layer-company-profiles
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/grants/browse/__tests__/grant-card.test.ts
  - src/app/grants/browse/page.tsx
  - src/app/grants/browse/grant-card.tsx
  - src/app/grants/browse/filters.tsx
  - src/app/grants/browse/grant-list.tsx
autonomous: true
requirements: [BRWSE-01, BRWSE-02, BRWSE-03, BRWSE-04]

must_haves:
  truths:
    - "User can visit /grants/browse without logging in and see a list of active grant cards"
    - "User can search grants by name using a text input"
    - "User can filter grants by provider agency, max funding range, and deadline"
    - "Each grant card displays name, provider, funding amount (formatted), deadline, short description, and an Aplica CTA button"
    - "Empty state shows a Romanian message when no grants match filters"
    - "Filters are preserved in URL search params for shareability and back-button support"
  artifacts:
    - path: "src/app/grants/browse/__tests__/grant-card.test.ts"
      provides: "Behavioral tests for GrantCard rendering, null handling, and deadline badge"
      min_lines: 40
    - path: "src/app/grants/browse/page.tsx"
      provides: "Server Component grant browse page with Supabase query"
      min_lines: 30
    - path: "src/app/grants/browse/grant-card.tsx"
      provides: "Reusable grant card component"
      exports: ["GrantCard"]
    - path: "src/app/grants/browse/filters.tsx"
      provides: "Client component: search and filter controls"
      exports: ["GrantFilters"]
    - path: "src/app/grants/browse/grant-list.tsx"
      provides: "Grant list with empty state"
      exports: ["GrantList"]
  key_links:
    - from: "src/app/grants/browse/page.tsx"
      to: "src/lib/supabase/server.ts"
      via: "createClient() for server-side Supabase query"
      pattern: "createClient.*from.*grants.*select"
    - from: "src/app/grants/browse/page.tsx"
      to: "supabase grants table"
      via: ".from('grants').select().eq('status', 'active')"
      pattern: "from\\('grants'\\)"
    - from: "src/app/grants/browse/filters.tsx"
      to: "URL searchParams"
      via: "useRouter/useSearchParams for filter state"
      pattern: "useSearchParams|searchParams"
    - from: "src/app/grants/browse/__tests__/grant-card.test.ts"
      to: "src/app/grants/browse/grant-card.tsx"
      via: "imports and renders GrantCard with test data"
      pattern: "import.*GrantCard.*from.*grant-card"
---

<objective>
Build the public grant browse page at /grants/browse where anyone (no auth required) can search and filter the grant catalog. Displays active grants as cards with name, provider, funding, deadline, description, and "Aplica" call-to-action. Uses server-side Supabase queries with URL search params for filter state.

Purpose: This is the public-facing grant catalog that lets users discover available grants before even creating a profile. It's a key acquisition page and works independently of the profile creation flow.

Output: Working /grants/browse page with search, filters, and grant cards, backed by behavioral tests for GrantCard rendering.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer-company-profiles/02-RESEARCH.md

<interfaces>
<!-- From Phase 1 -->

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From supabase/migrations/001_initial_schema.sql (grants table):
```sql
CREATE TABLE public.grants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  provider_agency TEXT NOT NULL,
  description TEXT,
  max_funding NUMERIC,
  currency TEXT DEFAULT 'MDL',
  deadline TIMESTAMPTZ,
  status TEXT CHECK (status IN ('draft', 'active', 'expired')) DEFAULT 'draft',
  ...
);
-- RLS: anon+authenticated can read active grants
CREATE POLICY "grants_select_active" ON public.grants FOR SELECT
  TO anon, authenticated USING (status = 'active');
-- Index for filtering
CREATE INDEX idx_grants_status_deadline ON public.grants (status, deadline);
```

From supabase/seed.sql (3 seed grants):
- AIPA agriculture grant (600,000 MDL, active)
- ODA digital grant (500,000 MDL, active)
- EU4Moldova green energy grant (1,000,000 EUR, active)

From src/app/globals.css (theme):
- Brand accent: oklch(0.7 0.15 60) warm orange
- shadcn theme variables
- Mobile-first, max-w-5xl container pattern

Existing shadcn/ui components: button.tsx
Need: card, input, badge, select (install if not already present from Plan 03)

<!-- From Plan 02-01 (vitest setup) -->
vitest.config.ts exists with path aliases (@/ mapped to src/)
Test runner: `npx vitest run --reporter=verbose`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create GrantCard behavioral tests</name>
  <files>src/app/grants/browse/__tests__/grant-card.test.ts</files>
  <behavior>
    - Test 1: GrantCard renders all fields (name, provider_agency as badge, formatted funding with currency, formatted deadline, description, "Aplica" CTA link)
    - Test 2: GrantCard handles null fields -- null description shows "Fara descriere", null max_funding shows "Suma necunoscuta", null deadline shows "Fara termen limita"
    - Test 3: GrantCard shows red "Expira curand" badge when deadline is within 14 days of current date
    - Test 4: GrantCard does NOT show "Expira curand" badge when deadline is more than 14 days away
  </behavior>
  <action>
    Create `src/app/grants/browse/__tests__/grant-card.test.ts`. Install `@testing-library/react` and `@testing-library/jest-dom` if not already present (`npm install -D @testing-library/react @testing-library/jest-dom`).

    Define a `Grant` type inline (or import from grant-card.tsx once Task 1 creates it -- since this is TDD, define it inline for now matching the grants table shape: `{ id: string, name: string, provider_agency: string, description: string | null, max_funding: number | null, currency: string, deadline: string | null }`).

    Create test fixtures:
    - `fullGrant`: all fields populated (name: "AIPA Agricultura", provider_agency: "AIPA", description: "Grant pentru agricultura", max_funding: 600000, currency: "MDL", deadline: ISO string 30 days from now)
    - `nullFieldsGrant`: description null, max_funding null, deadline null
    - `expiringGrant`: deadline set to 7 days from now (within 14-day threshold)
    - `safeGrant`: deadline set to 60 days from now (outside 14-day threshold)

    Write 4 tests using `@testing-library/react` render + screen queries:
    1. Render fullGrant, assert: name text present, provider_agency text present, "600" appears in formatted funding, "MDL" present, description text present, link with "Aplica" text exists.
    2. Render nullFieldsGrant, assert: "Fara descriere" present, "Suma necunoscuta" present, "Fara termen limita" present.
    3. Render expiringGrant, assert: "Expira curand" text present in the document.
    4. Render safeGrant, assert: "Expira curand" text NOT present in the document.

    These tests will FAIL initially (RED phase) because grant-card.tsx does not exist yet. That is expected -- Task 1 implements the component to make them pass (GREEN phase).
  </action>
  <verify>
    <automated>npx vitest run src/app/grants/browse/__tests__/grant-card.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with 4 behavioral tests. Tests fail with import error (grant-card.tsx not yet created) -- this is the expected RED state for TDD.</done>
</task>

<task type="auto">
  <name>Task 1: Create grant card component and filter controls</name>
  <files>src/app/grants/browse/grant-card.tsx, src/app/grants/browse/filters.tsx</files>
  <action>
    Install any needed shadcn components not yet present: `npx shadcn@latest add card input badge select` (will skip already-installed ones).

    1. Create `src/app/grants/browse/grant-card.tsx`:
       - Define and export a `Grant` TypeScript interface matching the grants table fields used in display: `{ id: string, name: string, provider_agency: string, description: string | null, max_funding: number | null, currency: string, deadline: string | null }`.
       - Export `GrantCard({ grant }: { grant: Grant })` component.
       - Use shadcn Card component. Layout:
         - Card header: grant name (font-semibold text-lg) and provider_agency as a subtle Badge.
         - Card body: description text (truncated to 2 lines with line-clamp-2), funding amount formatted with Intl.NumberFormat('ro-MD') + currency (e.g., "600,000 MDL"), deadline formatted with date-fns or Intl.DateTimeFormat('ro-MD') showing relative info (e.g., "Termen: 15 iunie 2026" or "Expira in 30 zile").
         - Card footer: "Aplica" button (Link to future grant application page -- for now link to `#` or `/grants/${grant.id}` as placeholder).
       - Show a red "Expira curand" badge if deadline is within 14 days.
       - Handle null description (show "Fara descriere"), null max_funding (show "Suma necunoscuta"), null deadline (show "Fara termen limita").
       - Mobile: card takes full width, stacks vertically.

    2. Create `src/app/grants/browse/filters.tsx` ('use client'):
       - Export `GrantFilters` component.
       - Uses `useSearchParams` from 'next/navigation' to read current filter state and `useRouter` to push updated params.
       - Controls:
         - Search input: placeholder "Cauta granturi...", debounced (300ms) URL param `q`. Use a simple useState + useEffect with setTimeout for debounce (no external debounce lib needed).
         - Provider dropdown: "Toti furnizorii" default, populated from a `providers` prop (array of unique provider_agency values passed from server component). URL param `provider`.
         - Max funding input: "Suma maxima (MDL)" number input. URL param `maxFunding`.
         - Deadline filter: "Pana la data" date input. URL param `deadline`.
       - When any filter changes, update URL search params using `router.push` with the new params (this triggers server component re-render with new data).
       - Include a "Reseteaza filtrele" button that clears all params.
       - Layout: horizontal flex-wrap on desktop, stacked on mobile.

    After creating both files, run the GrantCard tests from Task 0. All 4 tests should now PASS (GREEN phase).
  </action>
  <verify>
    <automated>npx vitest run src/app/grants/browse/__tests__/ --reporter=verbose</automated>
  </verify>
  <done>GrantCard renders all grant fields with proper formatting and null handling. All 4 GrantCard behavioral tests pass. GrantFilters manages URL search params with debounced search, provider dropdown, funding, and deadline filters. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create grant list component and assemble the browse page</name>
  <files>src/app/grants/browse/grant-list.tsx, src/app/grants/browse/page.tsx</files>
  <action>
    1. Create `src/app/grants/browse/grant-list.tsx`:
       - Export `GrantList({ grants }: { grants: Grant[] })` component.
       - If grants array is empty, show an empty state: an icon (Search from lucide-react), heading "Nu am gasit granturi", subtext "Incearca sa modifici filtrele sau sa cauti altceva".
       - If grants exist, render a responsive grid: 1 column on mobile (< md), 2 columns on md, 3 columns on lg. Each cell renders a GrantCard.
       - Include a count line above the grid: "X granturi disponibile" (or "1 grant disponibil" for singular).

    2. Create `src/app/grants/browse/page.tsx` (Server Component):
       - This is an async Server Component that reads searchParams.
       - Accept `searchParams` prop (Next.js 16 pattern: `searchParams: Promise<{ q?: string, provider?: string, maxFunding?: string, deadline?: string }>`). Await the promise to get params.
       - Create Supabase server client via `createClient()`.
       - Build query: `supabase.from('grants').select('id, name, provider_agency, description, max_funding, currency, deadline').eq('status', 'active').order('deadline', { ascending: true })`.
       - Apply filters conditionally:
         - If `q`: `.ilike('name', '%${q}%')` (search by name).
         - If `provider`: `.eq('provider_agency', provider)`.
         - If `maxFunding`: `.lte('max_funding', parseInt(maxFunding))`.
         - If `deadline`: `.lte('deadline', deadline)`.
       - Also fetch distinct providers for the filter dropdown: `supabase.from('grants').select('provider_agency').eq('status', 'active')` then deduplicate in JS.
       - Page layout:
         - Heading: "Granturi disponibile" (text-2xl font-bold).
         - Subheading: "Descopera oportunitatile de finantare disponibile pentru afacerea ta".
         - GrantFilters component with providers list.
         - GrantList component with fetched grants.
         - Wrapper: max-w-5xl mx-auto px-4 py-8.
       - Handle Supabase errors gracefully: show a Romanian error message if query fails.
       - Page metadata: `export const metadata = { title: 'Granturi disponibile | GrantAssist Moldova' }`.
  </action>
  <verify>
    <automated>npx vitest run src/app/grants/browse/__tests__/ --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>/grants/browse page renders with server-side Supabase query, shows grant cards for seed data (3 grants), search input filters by name, provider dropdown filters, funding and deadline filters update URL params and re-filter. Empty state shown when no matches. Mobile responsive grid. All GrantCard tests still green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/app/grants/browse/__tests__/ --reporter=verbose` -- all 4 GrantCard tests pass
- `npx tsc --noEmit` -- zero TypeScript errors
- `npm run build` -- Next.js build succeeds
- `/grants/browse` accessible without authentication (RLS grants_select_active allows anon)
- Seed grants (AIPA, ODA, EU4Moldova) display as cards
- Search by name filters correctly
- Provider dropdown filters correctly
- Empty state shows when no grants match
- URL search params preserved across navigation
</verification>

<success_criteria>
- /grants/browse loads active grants from Supabase without auth
- Grant cards show name, provider, funding (formatted), deadline, description, and Aplica CTA
- Search by name works with debounced input
- Filters by provider, max funding, and deadline work via URL params
- Empty state shows Romanian message when no matches
- Responsive grid: 1col mobile, 2col md, 3col lg
- "Expira curand" badge on grants with deadline within 14 days
- All 4 GrantCard behavioral tests pass via vitest
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer-company-profiles/02-04-SUMMARY.md`
</output>
