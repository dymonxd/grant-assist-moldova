---
phase: 05-ai-grant-writer-and-export
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/lib/pdf/application-pdf.tsx
  - src/lib/pdf/__tests__/application-pdf.test.ts
  - src/app/api/writer/pdf/route.ts
  - src/app/actions/export.ts
  - src/app/actions/__tests__/export.test.ts
  - src/lib/email/application-email.tsx
  - src/app/grants/[grantId]/write/export-bar.tsx
  - src/components/writer/__tests__/export-bar.test.ts
autonomous: true
requirements: [EXPRT-01, EXPRT-02, EXPRT-03, EXPRT-04, EXPRT-05, EXPRT-06]
user_setup:
  - service: resend
    why: "Email delivery for grant application export"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Create free account at resend.com"
        location: "https://resend.com/signup"
      - task: "Use onboarding@resend.dev for development (no domain needed)"
        location: "Automatic with free account"

must_haves:
  truths:
    - "User can click Copiaza tot and have all section text copied to clipboard"
    - "User can click Descarca PDF and download a PDF with all sections rendered with Romanian diacritics (ă, ș, ț) displayed correctly"
    - "User can click Trimite pe email and receive the application via email with formatted sections"
    - "Unauthenticated users can copy to clipboard but see AccountWallModal when clicking PDF or email"
    - "User can opt in to deadline reminders via a checkbox on the export bar"
    - "Required documents checklist with completion status is visible in export context"
  artifacts:
    - path: "src/lib/pdf/application-pdf.tsx"
      provides: "React-pdf Document component with Geist Sans Font.register for Romanian diacritics"
      exports: ["ApplicationPdfDocument"]
    - path: "src/app/api/writer/pdf/route.ts"
      provides: "POST Route Handler for server-side PDF generation and download"
      exports: ["POST"]
    - path: "src/app/actions/export.ts"
      provides: "sendApplicationEmail server action and saveReminderOptIn action"
      exports: ["sendApplicationEmail", "saveReminderOptIn"]
    - path: "src/lib/email/application-email.tsx"
      provides: "HTML email template for application sections"
      exports: ["buildApplicationEmailHtml"]
    - path: "src/app/grants/[grantId]/write/export-bar.tsx"
      provides: "Export bar with copy/PDF/email buttons and auth gating"
      exports: ["ExportBar"]
  key_links:
    - from: "src/app/api/writer/pdf/route.ts"
      to: "src/lib/pdf/application-pdf.tsx"
      via: "imports ApplicationPdfDocument and renders with renderToBuffer"
      pattern: "renderToBuffer.*ApplicationPdfDocument"
    - from: "src/app/actions/export.ts"
      to: "resend"
      via: "Resend SDK emails.send()"
      pattern: "resend\\.emails\\.send"
    - from: "src/app/grants/[grantId]/write/export-bar.tsx"
      to: "src/components/auth/account-wall-modal.tsx"
      via: "imports AccountWallModal for unauthenticated PDF/email gating"
      pattern: "AccountWallModal"
---

<objective>
Export functionality: PDF download with Romanian diacritics, clipboard copy, email via Resend, auth gating for PDF/email, deadline reminder opt-in.

Purpose: Completes the writer flow by allowing users to export their finished applications. PDF uses @react-pdf/renderer with Geist Sans font for proper Romanian character rendering. Email uses Resend SDK. Copy is client-side clipboard API. Auth gate reuses the existing AccountWallModal from Phase 4.

Output: Export bar component with copy/PDF/email buttons, server-side PDF generation route, email server action via Resend, and proper auth gating.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-grant-writer-and-export/05-RESEARCH.md
@.planning/phases/05-ai-grant-writer-and-export/05-01-SUMMARY.md
@.planning/phases/05-ai-grant-writer-and-export/05-02-SUMMARY.md

<interfaces>
<!-- Contracts from Phase 4 and Plans 01-02 that this plan consumes -->

From src/components/auth/account-wall-modal.tsx (Phase 4):
```typescript
export function AccountWallModal({
  open,
  onOpenChange,
  grantId,
}: {
  open: boolean
  onOpenChange: (open: boolean) => void
  grantId: string
}): JSX.Element
```

From src/app/api/pdf/test/route.ts (Phase 1 -- reference for react-pdf pattern):
```typescript
// Uses @react-pdf/renderer: Document, Page, Text, View, StyleSheet, renderToBuffer
// Currently uses Helvetica (no diacritics) -- Phase 5 MUST register Geist Sans
```

Font files available at:
```
node_modules/geist/dist/fonts/geist-sans/Geist-Regular.ttf
node_modules/geist/dist/fonts/geist-sans/Geist-Bold.ttf
```
WARNING: Do NOT use Geist-Italic[wght].ttf (variable font) -- PDF spec does not support it.

From supabase schema -- notifications_log:
```sql
CREATE TABLE public.notifications_log (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES public.profiles(id) ON DELETE CASCADE NOT NULL,
  grant_id UUID REFERENCES public.grants(id) ON DELETE SET NULL,
  type TEXT CHECK (type IN ('deadline_reminder', ...)) NOT NULL,
  channel TEXT CHECK (channel IN ('email', 'sms')) NOT NULL,
  sent_at TIMESTAMPTZ DEFAULT now()
);
```

Section data shape (from writer-client state):
```typescript
type SectionForExport = {
  fieldLabel: string
  finalText: string
}
```

Existing patterns:
- createAdminClient() for privileged DB operations
- navigator.clipboard.writeText for clipboard (used in share feature)
- Server actions return { error: string } on failure
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PDF document component with Geist Sans font, PDF route handler, and email server action</name>
  <files>
    src/lib/pdf/application-pdf.tsx,
    src/lib/pdf/__tests__/application-pdf.test.ts,
    src/app/api/writer/pdf/route.ts,
    src/lib/email/application-email.tsx,
    src/app/actions/export.ts,
    src/app/actions/__tests__/export.test.ts
  </files>
  <behavior>
    - ApplicationPdfDocument: React-pdf component rendering grant name, provider, all sections with labels and text, and generation timestamp
    - PDF Route Handler: accepts POST with { grantName, providerAgency, sections } and returns application/pdf binary response
    - buildApplicationEmailHtml: returns HTML string with grant name header, sections formatted with h3 labels and paragraph text, separated by hr
    - sendApplicationEmail: sends email via Resend to specified address with application content
    - sendApplicationEmail: returns error if RESEND_API_KEY is not configured
    - saveReminderOptIn: inserts a deadline_reminder row into notifications_log for the user and grant
    - saveReminderOptIn: returns error if user is not authenticated
  </behavior>
  <action>
    **Tests first (RED):**

    `src/lib/pdf/__tests__/application-pdf.test.ts`: Test that ApplicationPdfDocument is a valid React element when called with props (grantName, providerAgency, sections). Test that it includes Romanian diacritics text in the section data. NOTE: Do not test actual PDF rendering (requires full react-pdf runtime) -- test the component's props interface and that it does not throw on construction.

    `src/app/actions/__tests__/export.test.ts`: Mock Resend SDK. Test sendApplicationEmail: calls resend.emails.send with correct to/subject/html, returns success. Test sendApplicationEmail: returns error when Resend returns error. Test saveReminderOptIn: inserts into notifications_log with correct fields for authenticated user. Test saveReminderOptIn: returns error for unauthenticated user.

    **Implementation (GREEN):**

    `src/lib/pdf/application-pdf.tsx`:
    - Import Font, Document, Page, Text, View, StyleSheet from '@react-pdf/renderer'
    - Import path from 'path'
    - Font.register: family 'Geist Sans', two fonts:
      - Regular: `path.join(process.cwd(), 'node_modules/geist/dist/fonts/geist-sans/Geist-Regular.ttf')`
      - Bold: `path.join(process.cwd(), 'node_modules/geist/dist/fonts/geist-sans/Geist-Bold.ttf')` with fontWeight 'bold'
    - IMPORTANT per Research Pitfall 2: For Vercel deployment, ALSO copy the font files to `public/fonts/` as a fallback. Add a comment documenting this. For the initial implementation, use the node_modules path which works locally.
    - StyleSheet: page padding 40, fontFamily 'Geist Sans', fontSize 11, lineHeight 1.6; title fontSize 18 bold; sectionTitle fontSize 13 bold; text fontSize 11 color #333; footer fontSize 9 color #999
    - Export ApplicationPdfDocument component with props: `{ grantName: string; providerAgency: string; sections: Array<{ fieldLabel: string; finalText: string }> }`
    - Renders: Page with grant name title, provider subtitle, then each section as sectionTitle + text, footer with "Generat cu GrantAssist Moldova" and ISO timestamp

    `src/app/api/writer/pdf/route.ts`:
    - Export POST handler
    - Parse body: { grantName, providerAgency, sections }
    - Validate: grantName and sections required, return 400 if missing
    - Create React element of ApplicationPdfDocument with props
    - Call renderToBuffer to generate PDF
    - Return Response with Buffer, Content-Type: application/pdf, Content-Disposition: attachment; filename="cerere-{sanitized-grant-name}.pdf"

    `src/lib/email/application-email.tsx`:
    - Export buildApplicationEmailHtml function accepting { grantName, sections }
    - Returns HTML string (NOT a React Email component -- keep it simple with template literals)
    - Structure: h2 with grant name, then for each section: h3 with field label, p with text, hr separator
    - Footer: "Generat cu GrantAssist Moldova" with timestamp
    - Inline styles for email compatibility (no external CSS)

    `src/app/actions/export.ts`:
    - 'use server' directive
    - Import Resend from 'resend' (install it: the package.json doesn't have it yet -- add a note in action)
    - Export sendApplicationEmail(to: string, grantName: string, sections: Array<{ fieldLabel: string; finalText: string }>):
      1. Check RESEND_API_KEY exists, return { error: 'Serviciul de email nu este configurat' } if not
      2. Build HTML with buildApplicationEmailHtml
      3. Call resend.emails.send({ from: 'GrantAssist <onboarding@resend.dev>', to: [to], subject: `Cererea ta de finantare: ${grantName}`, html })
      4. Return { success: true } or { error: 'Eroare la trimiterea emailului' }
    - Export saveReminderOptIn(grantId: string):
      1. Check auth via createClient + getUser() -- return { error } if not authenticated
      2. Insert into notifications_log: user_id, grant_id, type: 'deadline_reminder', channel: 'email'
      3. Return { success: true }

    **NOTE: Run `npm install resend` before implementation.**
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run src/lib/pdf/__tests__/application-pdf.test.ts src/app/actions/__tests__/export.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - ApplicationPdfDocument renders with Geist Sans font registered for Romanian diacritics
    - PDF route handler generates downloadable PDF from section data
    - Email server action sends formatted application via Resend
    - Reminder opt-in persists to notifications_log for authenticated users
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Export bar component with copy/PDF/email buttons and auth gating</name>
  <files>
    src/app/grants/[grantId]/write/export-bar.tsx,
    src/components/writer/__tests__/export-bar.test.ts,
    src/app/grants/[grantId]/write/writer-client.tsx
  </files>
  <action>
    **`src/app/grants/[grantId]/write/export-bar.tsx`** ('use client') (EXPRT-01, EXPRT-04, EXPRT-05, EXPRT-06):
    - Props: `{ grantId: string; grantName: string; providerAgency: string; sections: Array<{ fieldLabel: string; finalText: string }>; isAuthenticated: boolean; requiredDocuments: string[] | null; checkedDocuments: Set<number> }`
    - Three export buttons in a Card:
      1. **"Copiaza tot"** (Copy icon): Uses navigator.clipboard.writeText. Concatenates all sections as "FieldLabel\n===\nFinalText\n\n---\n\n". Shows "Copiat!" state for 2 seconds via useState toggle. Works for ALL users (no auth gate).
      2. **"Descarca PDF"** (Download icon): If not authenticated, show AccountWallModal. If authenticated, POST to /api/writer/pdf with { grantName, providerAgency, sections }, receive blob, create download link via URL.createObjectURL + click.
      3. **"Trimite pe email"** (Mail icon): If not authenticated, show AccountWallModal. If authenticated, call sendApplicationEmail server action with user's email. Show loading state while sending, success/error message after.
    - AccountWallModal: Import from '@/components/auth/account-wall-modal'. Use useState for showModal. Pass grantId.
    - **Deadline reminder opt-in** (EXPRT-05): Checkbox below export buttons: "Notifica-ma cu 7 zile si 3 zile inainte de termenul limita". Only visible for authenticated users. On check, call saveReminderOptIn server action.
    - **Required documents summary** (EXPRT-06): Below the opt-in, show a summary of the document checklist completion: "X din Y documente pregatite". Use the checkedDocuments prop to count.

    **`src/components/writer/__tests__/export-bar.test.ts`**: Test: copy button calls navigator.clipboard.writeText with concatenated text. Test: PDF button shows AccountWallModal when not authenticated. Test: email button shows AccountWallModal when not authenticated. Test: reminder checkbox visible only for authenticated users.

    **Update `writer-client.tsx`**: Add ExportBar at the bottom of the writer (after DocumentChecklist). Pass isAuthenticated prop (received from page.tsx). Wire sections data (filter to only sections that have finalText). Pass checkedDocuments state.

    **Update `page.tsx`**: Fetch auth state via createClient + getUser(). Pass isAuthenticated boolean to WriterClient.
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - Copy button copies all sections to clipboard without auth requirement
    - PDF button gates behind auth, downloads PDF on click for authenticated users
    - Email button gates behind auth, sends email on click for authenticated users
    - AccountWallModal appears for unauthenticated PDF/email clicks
    - Deadline reminder opt-in checkbox visible for authenticated users
    - Document checklist completion summary shown
    - All tests pass including full suite
  </done>
</task>

</tasks>

<verification>
```bash
cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run --reporter=verbose
```
All tests pass. Export flow works: copy for all, PDF/email for authenticated, auth gate for anonymous.
</verification>

<success_criteria>
- "Copiaza tot" copies concatenated section text to clipboard for all users
- "Descarca PDF" generates and downloads PDF with Geist Sans font rendering Romanian diacritics correctly
- "Trimite pe email" sends formatted application email via Resend
- Unauthenticated users see AccountWallModal for PDF and email (copy works without auth)
- Deadline reminder opt-in checkbox persists preference to notifications_log
- Required documents completion summary visible
- Resend npm package installed
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-grant-writer-and-export/05-03-SUMMARY.md`
</output>
