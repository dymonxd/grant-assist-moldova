---
phase: 05-ai-grant-writer-and-export
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/app/settings/page.tsx
  - src/app/settings/__tests__/page.test.ts
  - src/app/actions/analytics.ts
  - src/app/actions/__tests__/analytics.test.ts
  - src/app/actions/settings.ts
  - src/app/actions/__tests__/settings.test.ts
autonomous: true
requirements: [GEN-05, GEN-06]
user_setup: []

must_haves:
  truths:
    - "User can visit /settings and toggle email notification preferences"
    - "Analytics events are tracked at each funnel stage via a server action"
    - "Settings page requires authentication -- unauthenticated users redirected"
  artifacts:
    - path: "src/app/settings/page.tsx"
      provides: "Notification preferences page with email opt-in/out toggle"
      exports: ["default"]
    - path: "src/app/actions/analytics.ts"
      provides: "trackEvent server action for analytics funnel tracking"
      exports: ["trackEvent"]
    - path: "src/app/actions/settings.ts"
      provides: "updateNotificationPreferences server action"
      exports: ["updateNotificationPreferences"]
  key_links:
    - from: "src/app/actions/analytics.ts"
      to: "analytics_events table"
      via: "createAdminClient insert"
      pattern: "analytics_events"
    - from: "src/app/actions/settings.ts"
      to: "profiles table"
      via: "update email_notifications column"
      pattern: "email_notifications"
---

<objective>
Settings page for notification preferences and analytics event tracking server action.

Purpose: Completes the two remaining GEN requirements for Phase 5 -- a notification preferences page where users can manage email opt-in/out (GEN-06), and a server action for analytics event tracking at each funnel stage (GEN-05). These run in parallel with Plan 03 since they depend only on Plan 02.

Output: Settings page at /settings, analytics tracking server action, notification preferences server action, with tests.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-grant-writer-and-export/05-RESEARCH.md

<interfaces>
<!-- Contracts from existing codebase -->

From supabase schema -- profiles:
```sql
CREATE TABLE public.profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  name TEXT,
  phone TEXT,
  is_admin BOOLEAN DEFAULT false,
  email_notifications BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

From supabase schema -- analytics_events:
```sql
CREATE TABLE public.analytics_events (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id TEXT NOT NULL,
  user_id UUID REFERENCES public.profiles(id) ON DELETE SET NULL,
  event_type TEXT NOT NULL,
  event_data JSONB,
  referrer_url TEXT,
  device_type TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);
-- RLS: admin-only SELECT and INSERT
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
// Uses request cookies for auth context
```

From src/lib/supabase/admin.ts:
```typescript
export function createAdminClient(): SupabaseClient
// Service role -- bypasses RLS
```

From src/lib/session.ts:
```typescript
export async function getSession(): Promise<IronSession<SessionData>>
// SessionData: { companyProfileId?: string }
```

Existing patterns:
- Auth check: createClient() -> supabase.auth.getUser() -> redirect if no user
- Server actions return { error: string } on failure, { success: true } on success
- Romanian UI text throughout
- useActionState from React 19 for forms
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Analytics tracking server action and notification settings server action</name>
  <files>
    src/app/actions/analytics.ts,
    src/app/actions/__tests__/analytics.test.ts,
    src/app/actions/settings.ts,
    src/app/actions/__tests__/settings.test.ts
  </files>
  <behavior>
    - trackEvent: inserts row into analytics_events with session_id, event_type, event_data, user_id (optional), referrer_url (optional)
    - trackEvent: uses createAdminClient because analytics_events RLS is admin-only INSERT
    - trackEvent: accepts event_type values like 'session_start', 'idno_entered', 'grants_viewed', 'account_created', 'writer_started', 'application_exported'
    - trackEvent: does not fail if optional fields are missing
    - updateNotificationPreferences: updates profiles.email_notifications for authenticated user
    - updateNotificationPreferences: returns error if user is not authenticated
    - updateNotificationPreferences: returns { success: true, emailNotifications: boolean } with new value
  </behavior>
  <action>
    **Tests first (RED):**

    `src/app/actions/__tests__/analytics.test.ts`: Mock createAdminClient. Test trackEvent: inserts into analytics_events with correct fields. Test trackEvent: handles optional user_id gracefully. Test trackEvent: handles insert failure with error return.

    `src/app/actions/__tests__/settings.test.ts`: Mock createClient and getUser. Test updateNotificationPreferences: updates email_notifications for authenticated user. Test updateNotificationPreferences: returns error for unauthenticated user. Test updateNotificationPreferences: returns new preference value.

    **Implementation (GREEN):**

    `src/app/actions/analytics.ts`:
    - 'use server' directive
    - Import createAdminClient (RLS is admin-only on analytics_events)
    - Import getSession for session_id fallback
    - Export trackEvent({ eventType, eventData?, userId?, referrerUrl?, deviceType? }):
      1. Get session for session_id (use companyProfileId or generate from cookie session)
      2. Insert into analytics_events via admin client: session_id (from iron-session cookie name or companyProfileId as identifier), user_id (null if anonymous), event_type, event_data (JSONB), referrer_url, device_type
      3. Return { success: true } or { error: string }
    - Funnel stages to track: 'session_start', 'idno_entered', 'idea_entered', 'profile_created', 'grants_viewed', 'account_created', 'writer_started', 'section_generated', 'section_saved', 'application_exported'

    `src/app/actions/settings.ts`:
    - 'use server' directive
    - Import createClient from supabase/server
    - Export updateNotificationPreferences(emailNotifications: boolean):
      1. createClient -> getUser() -- return { error: 'Trebuie sa fiti autentificat' } if no user
      2. Update profiles set email_notifications where id = user.id
      3. Return { success: true, emailNotifications }
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run src/app/actions/__tests__/analytics.test.ts src/app/actions/__tests__/settings.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - trackEvent inserts analytics events with session and event data using admin client
    - updateNotificationPreferences toggles email_notifications for authenticated users
    - Both actions handle error cases properly
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings page UI with notification preferences</name>
  <files>
    src/app/settings/page.tsx,
    src/app/settings/__tests__/page.test.ts
  </files>
  <action>
    **`src/app/settings/page.tsx`** (Server Component with Client form) (GEN-06):
    - Auth gate: createClient -> getUser() -> redirect('/') if not authenticated
    - Fetch current profile: query profiles table for email_notifications value
    - Render page with:
      - Title: "Setari" (h1)
      - Section: "Notificari" (h2)
      - Description: "Gestioneaza preferintele tale de notificare prin email."
      - Create a client component (inline or separate) for the toggle form:
        - Checkbox with label "Primeste notificari prin email" (deadline reminders, new grants, etc.)
        - Pre-checked based on current email_notifications value
        - On change: call updateNotificationPreferences server action
        - Show success/error state: "Preferintele au fost salvate" / error message
      - Use Card, CardContent, CardHeader, CardTitle from shadcn
      - Use Checkbox and Label from shadcn
      - Use Tailwind max-w-2xl mx-auto for centered layout
      - Mobile-responsive
    - Back link to home page

    **`src/app/settings/__tests__/page.test.ts`**: Test the settings page component renders notification checkbox, handles toggle.

    **Also:** Add analytics tracking calls to the writer flow. In `writer-client.tsx` (from Plan 02), call trackEvent({ eventType: 'writer_started' }) on mount. In ExportBar (from Plan 03), call trackEvent({ eventType: 'application_exported' }) on successful export. These are non-blocking fire-and-forget calls. Do NOT await them or show loading state -- analytics must not slow down the UX.
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - /settings page shows notification preferences for authenticated users
    - Unauthenticated users redirected to home
    - Email notification toggle persists preference
    - Analytics events tracked at writer_started and application_exported stages
    - All tests pass including full suite
  </done>
</task>

</tasks>

<verification>
```bash
cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run --reporter=verbose
```
All tests pass. Settings page renders correctly. Analytics events fire without blocking UX.
</verification>

<success_criteria>
- /settings page shows notification preferences with working toggle for authenticated users
- Unauthenticated users redirected from /settings
- trackEvent server action inserts analytics events at funnel stages
- Analytics tracked at writer_started and application_exported stages
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-grant-writer-and-export/05-04-SUMMARY.md`
</output>
