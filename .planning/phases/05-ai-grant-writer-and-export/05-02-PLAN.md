---
phase: 05-ai-grant-writer-and-export
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/grants/[grantId]/write/page.tsx
  - src/app/grants/[grantId]/write/writer-client.tsx
  - src/app/grants/[grantId]/write/section-editor.tsx
  - src/app/grants/[grantId]/write/grant-header.tsx
  - src/components/writer/progress-bar.tsx
  - src/components/writer/document-checklist.tsx
  - src/components/writer/scoring-hints.tsx
  - src/components/writer/__tests__/progress-bar.test.ts
  - src/components/writer/__tests__/scoring-hints.test.ts
autonomous: true
requirements: [WRITE-03, WRITE-06, WRITE-07, WRITE-08, WRITE-09, WRITE-10, WRITE-11]
user_setup: []

must_haves:
  truths:
    - "User visits /grants/[grantId]/write and sees all grant application sections loaded from the database with grant summary and deadline countdown at top"
    - "User can type a brief answer in a section, click Genereaza cu AI, and see streaming Romanian text appear progressively"
    - "Each section shows character count with warning when near the limit"
    - "Each section has Editeaza, Regenereaza, Salveaza, and Urmatoarea buttons that change based on section state"
    - "Progress bar shows X din Y sectiuni completate updating as sections are saved"
    - "Scoring rubric hints are visible in a collapsible panel above each section field"
    - "Required documents checklist appears at the bottom of the page with checkable items"
  artifacts:
    - path: "src/app/grants/[grantId]/write/page.tsx"
      provides: "Server Component: deadline check, application creation, data loading"
      exports: ["default"]
    - path: "src/app/grants/[grantId]/write/writer-client.tsx"
      provides: "Client Component: orchestrates sections, progress, streaming state"
      exports: ["WriterClient"]
    - path: "src/app/grants/[grantId]/write/section-editor.tsx"
      provides: "Client Component: individual section with state machine and AI generation"
      exports: ["SectionEditor"]
    - path: "src/app/grants/[grantId]/write/grant-header.tsx"
      provides: "Client Component: grant summary with deadline countdown"
      exports: ["GrantHeader"]
    - path: "src/components/writer/progress-bar.tsx"
      provides: "Progress bar showing X din Y sectiuni completate"
      exports: ["ProgressBar"]
    - path: "src/components/writer/document-checklist.tsx"
      provides: "Required documents checklist with checkable items"
      exports: ["DocumentChecklist"]
    - path: "src/components/writer/scoring-hints.tsx"
      provides: "Collapsible rubric criteria display"
      exports: ["ScoringHints"]
  key_links:
    - from: "src/app/grants/[grantId]/write/page.tsx"
      to: "src/app/actions/writer.ts"
      via: "calls getOrCreateApplication server action"
      pattern: "getOrCreateApplication"
    - from: "src/app/grants/[grantId]/write/section-editor.tsx"
      to: "/api/writer/generate"
      via: "fetch + ReadableStream for streaming AI text"
      pattern: "fetch.*api/writer/generate"
    - from: "src/app/grants/[grantId]/write/section-editor.tsx"
      to: "src/app/actions/writer.ts"
      via: "calls saveSection server action"
      pattern: "saveSection"
---

<objective>
Writer page UI: section-by-section grant application editor with streaming AI generation, progress tracking, rubric hints, and document checklist.

Purpose: This is the core user-facing value of Phase 5 -- the page where users write their grant applications section by section with AI assistance. The backend from Plan 01 powers all data operations; this plan builds the interactive UI.

Output: Complete writer page at /grants/[grantId]/write with section editors, streaming generation, progress bar, scoring hints, document checklist, and grant summary header.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-grant-writer-and-export/05-RESEARCH.md
@.planning/phases/05-ai-grant-writer-and-export/05-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/app/actions/writer.ts (created in Plan 01):
```typescript
export async function getOrCreateApplication(grantId: string): Promise<
  | {
      application: { id: string; grant_id: string; status: string; field_snapshot: any }
      sections: Array<{
        id: string
        grant_field_id: string
        user_brief: string | null
        ai_draft: string | null
        final_text: string | null
        is_saved: boolean
      }>
      fields: Array<{
        id: string
        field_order: number
        field_label: string
        field_type: string
        is_required: boolean
        character_limit: number | null
        helper_text: string | null
      }>
      grant: {
        id: string
        name: string
        provider_agency: string
        deadline: string | null
        scoring_rubric: { criteria: Array<{ name: string; weight: number; description: string }> } | null
        required_documents: string[] | null
        max_funding: number | null
        currency: string
        description: string | null
      }
      isUrgent: boolean
    }
  | { error: string }
>

export async function saveSection(sectionId: string, finalText: string): Promise<
  { success: true } | { error: string }
>

export function checkDeadline(deadline: string | null): {
  status: 'expired' | 'urgent' | 'ok'
  daysLeft: number
}
```

Route Handler /api/writer/generate (created in Plan 01):
```
POST /api/writer/generate
Body: { sectionId, fieldLabel, helperText, characterLimit, scoringRubric, companyProfile, userBrief }
Response: text/plain streaming response (ReadableStream)
```

Existing UI components available:
- Card, CardContent, CardHeader, CardTitle from @/components/ui/card
- Button from @/components/ui/button
- Badge from @/components/ui/badge
- Checkbox from @/components/ui/checkbox
- Label from @/components/ui/label
- Input from @/components/ui/input
- Textarea from @/components/ui/textarea
- lucide-react icons: ChevronDown, ChevronUp, FileText, Clock, AlertTriangle, Check, Loader2, etc.

Existing patterns:
- useActionState from React 19 for form submissions
- useState for client-side state management
- Server Components for data loading, Client Components for interactivity
- Romanian UI copy throughout
- Tailwind for styling, mobile-first (375px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared writer components (progress bar, scoring hints, document checklist)</name>
  <files>
    src/components/writer/progress-bar.tsx,
    src/components/writer/scoring-hints.tsx,
    src/components/writer/document-checklist.tsx,
    src/components/writer/__tests__/progress-bar.test.ts,
    src/components/writer/__tests__/scoring-hints.test.ts
  </files>
  <action>
    Create three reusable writer components:

    **`src/components/writer/progress-bar.tsx`** (WRITE-09):
    - Props: `{ completedCount: number; totalCount: number }`
    - Renders: "X din Y sectiuni completate" text with a visual progress bar
    - Use a simple div-based bar (bg-muted for track, bg-primary for fill) -- do NOT install shadcn Progress component to avoid unnecessary dependency
    - Width percentage: `(completedCount / totalCount) * 100`
    - Show percentage text next to the bar
    - Mobile-responsive: full width on mobile

    **`src/components/writer/scoring-hints.tsx`** (WRITE-06):
    - Props: `{ criteria: Array<{ name: string; weight: number; description: string }> | null }`
    - Renders: collapsible panel with rubric criteria
    - Default state: collapsed (showing "Criterii de evaluare" header with ChevronDown)
    - Expanded: shows each criterion as "Name (Weight%): Description"
    - Use useState for toggle, not shadcn Accordion (keep it simple)
    - If criteria is null or empty, render nothing

    **`src/components/writer/document-checklist.tsx`** (WRITE-10):
    - Props: `{ documents: string[] | null; checkedIds: Set<number>; onToggle: (index: number) => void }`
    - Renders: "Documente necesare" header with checkable list items
    - Each item: Checkbox + document name text
    - State managed by parent (checkedIds + onToggle) for persistence flexibility
    - If documents is null or empty, render nothing

    **Tests:** Create `progress-bar.test.ts` testing: renders correct text "2 din 5 sectiuni completate", bar width style matches percentage, handles 0/0 gracefully. Create `scoring-hints.test.ts` testing: collapsed by default, expands on click, shows all criteria when expanded, renders nothing when criteria is null.
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run src/components/writer/__tests__/ --reporter=verbose</automated>
  </verify>
  <done>
    - ProgressBar shows "X din Y sectiuni completate" with visual bar
    - ScoringHints collapses/expands to show rubric criteria with weights
    - DocumentChecklist renders checkable list of required documents
    - Tests pass for progress bar and scoring hints
  </done>
</task>

<task type="auto">
  <name>Task 2: Writer page, grant header, section editor, and writer client</name>
  <files>
    src/app/grants/[grantId]/write/page.tsx,
    src/app/grants/[grantId]/write/grant-header.tsx,
    src/app/grants/[grantId]/write/section-editor.tsx,
    src/app/grants/[grantId]/write/writer-client.tsx
  </files>
  <action>
    **`src/app/grants/[grantId]/write/page.tsx`** (Server Component) (WRITE-11, WRITE-12):
    - Extract grantId from params
    - Call getOrCreateApplication(grantId)
    - If error returned: show error card (expired grant block with message, or generic error)
    - If expired: show dedicated "Grant expirat" block with grant name and message "Termenul limita pentru acest grant a expirat. Nu mai puteti depune cererea."
    - Pass { application, sections, fields, grant, isUrgent } to WriterClient
    - Also fetch company profile from session for AI context (pass to WriterClient)

    **`src/app/grants/[grantId]/write/grant-header.tsx`** ('use client') (WRITE-11):
    - Props: `{ grant: { name, provider_agency, deadline, max_funding, currency }, isUrgent: boolean }`
    - Renders: Card with grant name (title), provider (Badge), funding amount formatted with Intl.NumberFormat('ro-MD'), deadline with countdown
    - Deadline countdown: "X zile ramase" with Clock icon. If isUrgent: show AlertTriangle icon with yellow/orange warning text "Atentie: mai putin de 3 zile!"
    - Use Intl.DateTimeFormat('ro-MD') for deadline date display

    **`src/app/grants/[grantId]/write/section-editor.tsx`** ('use client') (WRITE-03, WRITE-04, WRITE-07, WRITE-08):
    - Props: `{ section, field, scoringRubric, companyProfile, onSave: (sectionId, text) => void }`
    - Section state machine with useState:
      - `idle`: Shows textarea for user brief input. "Genereaza cu AI" button disabled until input exists.
      - `generating`: Streaming AI text appears in a read-only display area. All buttons disabled. Show Loader2 spinner.
      - `drafted`: AI text shown. Buttons: "Editeaza" (switches to editing), "Regenereaza" (re-runs generation), "Salveaza" (persists), "Urmatoarea" (calls onSave then scrolls to next)
      - `editing`: Textarea with AI text editable. "Salveaza" button active.
      - `saved`: Section shows final text with check icon. "Editeaza" button to re-enter editing.
    - Streaming implementation (WRITE-03): Use fetch + ReadableStream reader (NOT @ai-sdk/react). Call `/api/writer/generate` with POST body containing field context + company profile + user brief. On each chunk, update displayed text via setState. Use AbortController for cleanup on unmount (prevent race condition per Research Pitfall 6).
    - Clarifying question (WRITE-04): When user brief is under 20 chars, the AI prompt already triggers a question (handled in Plan 01). Client-side: also show a subtle hint "Raspunsul este prea scurt -- AI-ul va pune o intrebare de clarificare" when input length < 20.
    - Character count (WRITE-07): Below the textarea/display, show "X / Y caractere" when field has character_limit. Yellow warning when > 90% of limit. Red when at limit. Do NOT truncate client-side -- just warn.
    - ScoringHints component rendered above the textarea (from Task 1)
    - Buttons in Romanian: "Genereaza cu AI", "Editeaza", "Regenereaza", "Salveaza", "Urmatoarea sectiune"

    **`src/app/grants/[grantId]/write/writer-client.tsx`** ('use client') (WRITE-09, WRITE-10):
    - Props: `{ application, sections, fields, grant, isUrgent, companyProfile }`
    - Orchestrates all sections, progress tracking, and document checklist
    - State: sections array (track is_saved per section), checkedDocuments Set
    - Renders:
      1. GrantHeader at top
      2. ProgressBar below header
      3. SectionEditor for each field (ordered by field_order), passing scoring rubric from grant
      4. DocumentChecklist at bottom
    - onSave callback: calls saveSection server action, updates local sections state (mark is_saved), recalculates progress count
    - Section 1 auto-preview (WRITE-02): On mount, if section 1 has no ai_draft and no final_text, trigger auto-generation by calling the streaming endpoint with an empty userBrief="Genereaza un text de previzualizare" (the system prompt handles this). Display result as grayed-out italic text in the section editor. Use useEffect with AbortController and a ref to prevent double-fire in Strict Mode.
    - Scroll behavior: "Urmatoarea sectiune" scrolls to the next section using element.scrollIntoView({ behavior: 'smooth' })
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - /grants/[grantId]/write loads grant data and renders section editors
    - Expired grants show block page, urgent grants show warning
    - Grant header shows name, provider, funding, deadline countdown
    - Section editor implements full state machine with streaming AI generation
    - Character count with visual warnings near limit
    - Scoring hints collapsible above each section
    - Progress bar updates as sections are saved
    - Document checklist with checkable items at bottom
    - Section 1 auto-previews with grayed-out AI text on first visit
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
```bash
cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run --reporter=verbose
```
All tests pass. Manually verify: navigate to /grants/{grantId}/write and confirm section editors render with all UI elements.
</verification>

<success_criteria>
- Writer page at /grants/[grantId]/write shows all sections from the database
- Grant header displays name, provider, funding, deadline countdown with urgent warning
- Section editor state machine works: idle -> generating (streaming) -> drafted -> saved
- Character count shows with warnings at 90% and 100% of limit
- Scoring hints expand/collapse above each section
- Progress bar accurately reflects saved section count
- Document checklist renders with checkable items
- Section 1 auto-previews on first visit
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-grant-writer-and-export/05-02-SUMMARY.md`
</output>
