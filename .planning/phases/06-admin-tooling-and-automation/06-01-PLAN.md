---
phase: 06-admin-tooling-and-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260322_admin_phase6.sql
  - src/proxy.ts
  - src/app/actions/admin-analytics.ts
  - src/app/actions/__tests__/admin-analytics.test.ts
  - src/app/admin/page.tsx
  - src/app/admin/layout.tsx
  - src/app/admin/components/funnel-bar.tsx
  - src/app/admin/components/activity-feed.tsx
  - src/app/admin/components/applications-table.tsx
  - src/app/admin/components/stage-detail.tsx
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06]

must_haves:
  truths:
    - "Admin user at /admin sees analytics funnel with 6 stages and conversion rates"
    - "Clicking a funnel stage shows daily trend, device breakdown, and top referrers"
    - "Recent activity feed shows last 50 analytics events"
    - "Active applications list shows user, grant, last activity, completion percentage"
    - "Stale applications (7+ days) have Send reminder action"
    - "Non-admin authenticated users are redirected away from /admin"
  artifacts:
    - path: "supabase/migrations/20260322_admin_phase6.sql"
      provides: "profiles.email column + backfill + trigger update; RLS policies for admin access to applications, company_profiles, application_sections; indexes for stale queries; notifications_log unsubscribe_token"
      contains: "CREATE POLICY"
    - path: "src/app/actions/admin-analytics.ts"
      provides: "Server actions for dashboard data: getFunnelData, getStageDetail, getRecentActivity, getApplicationsList"
      exports: ["getFunnelData", "getStageDetail", "getRecentActivity", "getApplicationsList"]
    - path: "src/app/admin/page.tsx"
      provides: "Admin dashboard page with funnel, activity feed, applications"
      min_lines: 40
    - path: "src/app/admin/layout.tsx"
      provides: "Admin layout with sidebar navigation"
      min_lines: 20
  key_links:
    - from: "src/app/admin/page.tsx"
      to: "src/app/actions/admin-analytics.ts"
      via: "server action calls"
      pattern: "getFunnelData|getRecentActivity|getApplicationsList"
    - from: "src/proxy.ts"
      to: "profiles.is_admin"
      via: "admin role check in middleware"
      pattern: "is_admin"
---

<objective>
Admin dashboard with analytics funnel, activity feed, and application management.

Purpose: Give administrators visibility into platform usage via a 6-stage conversion funnel (Sessions to IDNO Entered to Grants Viewed to Account Created to Writer Started to Exported), real-time activity monitoring, and the ability to identify stale applications that need reminders.

Output: Working /admin dashboard with funnel visualization, clickable stage details, recent activity feed, and active/stale application tables.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-tooling-and-automation/06-RESEARCH.md
@.planning/phases/06-admin-tooling-and-automation/06-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/proxy.ts (needs is_admin check added):
```typescript
// Currently only checks data?.claims exists -- does NOT verify is_admin
if (pathname.startsWith('/admin') && !data?.claims) {
  // redirect to /login
}
```

From src/app/actions/analytics.ts (existing trackEvent pattern):
```typescript
export async function trackEvent({ eventType, eventData, userId, referrerUrl, deviceType }: TrackEventParams)
```

From src/lib/supabase/admin.ts:
```typescript
export function createAdminClient()  // service role, bypasses RLS
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient()  // authenticated user context
```

DB tables used:
- analytics_events: session_id, user_id, event_type, event_data, referrer_url, device_type, created_at
- analytics_daily_summary: date, stage, count, device_type, top_referrers (JSONB) -- composite PK (date, stage, device_type)
- applications: user_id, grant_id, company_profile_id, status (in_progress/completed/exported), updated_at
- profiles: id, name, phone, is_admin, email_notifications (NOTE: no email column -- email lives in auth.users, accessed via admin.auth.admin.getUserById())
- notifications_log: user_id, application_id, grant_id, type, channel, sent_at
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB migration + admin auth hardening + analytics server actions</name>
  <files>
    supabase/migrations/20260322_admin_phase6.sql,
    src/proxy.ts,
    src/app/actions/admin-analytics.ts,
    src/app/actions/__tests__/admin-analytics.test.ts
  </files>
  <behavior>
    - getFunnelData returns array of 6 stages with label, count, and conversion rate from analytics_daily_summary (last 30 days aggregated)
    - getStageDetail(stage) returns { dailyTrend: {date, count}[], deviceBreakdown: {device, count}[], topReferrers: {url, count}[] } for last 30 days
    - getRecentActivity returns last 50 analytics_events with event_type, created_at, session_id
    - getApplicationsList returns applications joined with profiles and grants, with staleness flag (updated_at > 7 days ago)
    - sendStaleReminder(applicationId) inserts into notifications_log and sends email
    - All actions verify caller is_admin via createClient + getUser + profiles check; return { error } if not admin
    - proxy.ts rejects non-admin authenticated users from /admin/* (not just unauthenticated)
  </behavior>
  <action>
    1. Create SQL migration `supabase/migrations/20260322_admin_phase6.sql`:
       - Add `email TEXT` column to profiles table
       - Backfill email from auth.users: `UPDATE public.profiles SET email = u.email FROM auth.users u WHERE u.id = public.profiles.id`
       - Update handle_new_user trigger to also copy email: `NEW.email` (add to INSERT VALUES)
       - Add `unsubscribe_token UUID DEFAULT gen_random_uuid()` column to notifications_log
       - CREATE INDEX on notifications_log (user_id, grant_id, type, sent_at) for duplicate prevention
       - CREATE INDEX on applications (status, updated_at) for stale query
       - Admin SELECT policy on applications: `USING (is_admin())`
       - Admin SELECT policy on company_profiles: `USING (is_admin())`
       - Admin SELECT policy on application_sections: `USING (is_admin())`
       Use `CREATE POLICY IF NOT EXISTS` pattern for safety.

    2. Update `src/proxy.ts`: After the existing `getClaims()` check, for `/admin` routes where claims exist, do a lightweight Supabase query via the proxy's client: `supabase.from('profiles').select('is_admin').eq('id', data.claims.sub).single()`. If `is_admin !== true`, redirect to `/`. Keep the existing unauthenticated redirect to `/login`.

    3. Create `src/app/actions/admin-analytics.ts` with `'use server'` directive:
       - Helper `verifyAdmin()`: uses createClient + getUser, then queries profiles.is_admin. Returns user or throws/returns error.
       - `getFunnelData()`: Query analytics_daily_summary, aggregate last 30 days by stage. Map event_types to funnel labels: session_start -> "Sesiuni", idno_entered -> "IDNO introdus", grants_viewed -> "Granturi vizualizate", account_created -> "Cont creat", writer_started -> "Redactor pornit", application_exported -> "Exportat". Calculate conversion rates (stage count / previous stage count * 100).
       - `getStageDetail(stage: string)`: Query analytics_daily_summary filtered by stage for last 30 days. Group by date for dailyTrend. Group by device_type for deviceBreakdown. Aggregate top_referrers JSONB across days for topReferrers.
       - `getRecentActivity()`: Query analytics_events ORDER BY created_at DESC LIMIT 50. Return event_type, created_at, session_id, event_data.
       - `getApplicationsList()`: Query applications with status != 'exported', join profiles (name, email) and grants (name, deadline). Calculate `isStale = updated_at < now() - 7 days`. Calculate `completionPercent` from application_sections count vs grant_application_fields count. Order by updated_at DESC.
       - `sendStaleReminder(applicationId: string)`: Verify admin, fetch application + user + grant data, check notifications_log for recent abandoned_draft (last 24h), send email via Resend, log to notifications_log. Return { success } or { error }.

    4. Create tests in `src/app/actions/__tests__/admin-analytics.test.ts`:
       - Mock createClient and createAdminClient (use established chainable mock pattern from project)
       - Test getFunnelData returns 6 stages with correct labels and conversion rates
       - Test getStageDetail returns dailyTrend, deviceBreakdown, topReferrers
       - Test getRecentActivity returns last 50 events sorted desc
       - Test getApplicationsList flags stale applications correctly
       - Test verifyAdmin rejects non-admin users with error
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run src/app/actions/__tests__/admin-analytics.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    All 5+ admin analytics tests pass. Migration SQL file exists. proxy.ts checks is_admin for /admin routes. Server actions verify admin role before returning data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin dashboard UI with funnel, activity feed, and applications table</name>
  <files>
    src/app/admin/layout.tsx,
    src/app/admin/page.tsx,
    src/app/admin/components/funnel-bar.tsx,
    src/app/admin/components/activity-feed.tsx,
    src/app/admin/components/applications-table.tsx,
    src/app/admin/components/stage-detail.tsx
  </files>
  <action>
    1. Create `src/app/admin/layout.tsx`: Server component. Verify admin access (createClient + getUser + profiles.is_admin check). If not admin, redirect('/').
       - Sidebar nav with links: Dashboard (/admin), Granturi (/admin/grants), Notificari (/admin/notifications).
       - Use Tailwind: flex layout, sidebar w-56 bg-card border-r, main flex-1 p-6.
       - Mobile: sidebar collapses to top horizontal nav.

    2. Create `src/app/admin/components/funnel-bar.tsx`: Client component ('use client').
       - Props: `stages: { label: string, count: number, rate: number }[]`, `onStageClick: (stage: string) => void`
       - Render 6 horizontal bars with percentage widths relative to max count.
       - Each bar shows label, count, and conversion rate badge.
       - Color gradient: first bar full opacity, decreasing through funnel.
       - Tailwind: flexbox column, each bar is a div with dynamic width via style={{ width: `${percent}%` }}, min-w-16 for visibility.

    3. Create `src/app/admin/components/stage-detail.tsx`: Client component.
       - Props: `stage: string, data: { dailyTrend, deviceBreakdown, topReferrers } | null`
       - Daily trend: 30 vertical bars (div-based bar chart), each bar height proportional to count, hover shows date + count.
       - Device breakdown: simple list with count badges (Desktop, Mobile, Tablet).
       - Top referrers: ordered list with URL and count.
       - Loading state while data is null.

    4. Create `src/app/admin/components/activity-feed.tsx`: Server component or client.
       - Props: `events: { event_type: string, created_at: string, session_id: string }[]`
       - Render as scrollable list with event type badge, time (Intl.DateTimeFormat 'ro-MD'), session ID truncated.
       - Map event types to Romanian labels and color-coded badges.

    5. Create `src/app/admin/components/applications-table.tsx`: Client component.
       - Props: `applications: ApplicationListItem[]`, `onSendReminder: (id: string) => void`
       - Table with columns: Utilizator, Grant, Ultima activitate, Progres (%), Status.
       - Stale rows (isStale=true) highlighted with orange bg. Show "Trimite memento" button for stale.
       - Progres shows div-based progress bar with percentage.
       - Sort by last activity descending.

    6. Update `src/app/admin/page.tsx`: Server component.
       - Call getFunnelData(), getRecentActivity(), getApplicationsList() at top level.
       - Render AdminDashboardClient (client component for interactivity) passing server data.
       - AdminDashboardClient manages selected funnel stage state. On stage click, calls getStageDetail(stage) and renders StageDetail.
       - Section headings in Romanian: "Analiza conversiilor", "Activitate recenta", "Aplicatii active".
       - Layout: funnel at top full-width, then two-column grid (activity feed left, applications right) on desktop, stacked on mobile.
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Admin dashboard renders at /admin with 6-stage funnel bars, clickable stages showing detail panel, scrollable activity feed with last 50 events, and applications table with stale highlighting and reminder button. Build succeeds without errors.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/app/actions/__tests__/admin-analytics.test.ts` passes
- `npx next build` succeeds
- Migration SQL is syntactically valid
- proxy.ts checks is_admin for /admin routes
</verification>

<success_criteria>
Admin user at /admin sees a working analytics funnel with 6 labeled stages, conversion rates between stages, clickable stage detail with daily trends and device breakdown, a recent activity feed, and an applications table highlighting stale entries with reminder capability. Non-admin users are redirected.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-tooling-and-automation/06-01-SUMMARY.md`
</output>
