---
phase: 06-admin-tooling-and-automation
plan: 06
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05"]
files_modified: []
autonomous: false
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06, AGRANT-01, AGRANT-02, AGRANT-03, AGRANT-04, AGRANT-05, AGRANT-06, AGRANT-07, AGRANT-08, AGRANT-09, AGRANT-10, ANOTIF-01, ANOTIF-02, ANOTIF-03, AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05, AUTO-06, AUTO-07]

must_haves:
  truths:
    - "Admin at /admin sees analytics funnel, activity feed, and application management"
    - "Admin at /admin/grants can manage grant catalog with status badges, inline editing, and Re-scrape"
    - "Admin at /admin/grants/new can create grants via 3-step wizard with AI PDF extraction"
    - "Admin at /admin/notifications can view log, filter by type, and bulk-send with confirmation"
    - "Non-admin users are redirected away from all /admin routes"
    - "Automated cron jobs correctly target users for deadline reminders and abandoned draft nudges"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification checkpoint for Phase 6.

Purpose: Verify all 26 requirements are satisfied by running the full test suite, building the project, and having the user visually verify the admin dashboard, grant management, and notification interfaces.

Output: Verified Phase 6 completion.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-tooling-and-automation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build verification</name>
  <files>
    src/app/actions/__tests__/admin-analytics.test.ts,
    src/app/actions/__tests__/admin-grants.test.ts,
    src/app/actions/__tests__/admin-notifications.test.ts,
    src/lib/ai/__tests__/extract-grant-pdf.test.ts,
    src/app/api/cron/__tests__/cron.test.ts
  </files>
  <action>
    1. Run `npx vitest run --reporter=verbose` to execute all tests including:
       - src/app/actions/__tests__/admin-analytics.test.ts
       - src/app/actions/__tests__/admin-grants.test.ts
       - src/app/actions/__tests__/admin-notifications.test.ts
       - src/lib/ai/__tests__/extract-grant-pdf.test.ts
       - src/app/api/cron/__tests__/cron.test.ts
       - All existing tests from Phases 1-5

    2. Run `npx next build` to verify full production build succeeds.

    3. If any test fails or build errors occur, diagnose and fix the issue.
       - For test failures: check mock patterns, import paths, type mismatches.
       - For build failures: check for missing imports, type errors, invalid JSX.
  </action>
  <verify>
    <automated>cd "/Users/dymonxd/Desktop/Agensy 🤖 /GrantsAI/Grands (new)" && npx vitest run --reporter=verbose && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    All tests pass. Production build succeeds with no errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of admin UI and access control</name>
  <what-built>Complete admin tooling built across Plans 01-05: analytics dashboard, grant catalog with Re-scrape, grant wizard with AI extraction, notification log with bulk send, and automated cron jobs.</what-built>
  <how-to-verify>
    1. /admin renders funnel, activity feed, applications table
    2. /admin/grants renders catalog table with status badges
    3. /admin/grants table actions dropdown includes Re-extragere (Re-scrape) for grants with source URL
    4. /admin/grants/new renders 3-step wizard
    5. /admin/notifications renders log with filters and bulk send
    6. Unauthenticated users redirected from /admin to /login
    7. Non-admin users redirected from /admin to /
    8. Sidebar navigation works between admin sections
    9. All Romanian labels render correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All tests pass with `npx vitest run`
- `npx next build` succeeds
- Human verifies admin UI renders correctly
- Human verifies admin access control works
</verification>

<success_criteria>
Phase 6 is complete when all tests pass, build succeeds, and the user confirms the admin dashboard, grant management wizard, notification log, and access controls work as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-tooling-and-automation/06-06-SUMMARY.md`
</output>
