---
phase: 03-grant-matching
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/grants/score-badge.tsx
  - src/components/grants/hero-card.tsx
  - src/components/grants/match-card.tsx
  - src/components/grants/improvement-tips.tsx
  - src/components/grants/match-list.tsx
  - src/components/grants/profile-sidebar.tsx
  - src/components/grants/results-layout.tsx
  - src/components/grants/__tests__/hero-card.test.ts
  - src/components/grants/__tests__/match-card.test.ts
  - src/components/grants/__tests__/profile-sidebar.test.ts
autonomous: true
requirements:
  - MATCH-03
  - MATCH-05
  - MATCH-06

must_haves:
  truths:
    - "Top recommendation appears as a hero card with grant name, provider, score, funding, deadline, and AI explanation"
    - "Each grant card has Aplica acum and Salveaza action buttons"
    - "Sub-50% matches show improvement suggestions in a yellow info box"
    - "Profile sidebar is visible on desktop and collapsible on mobile with company data and edit link"
    - "MatchList renders hero card for top match and compact cards for the rest"
  artifacts:
    - path: "src/components/grants/hero-card.tsx"
      provides: "Top match hero display with score, explanation, funding, deadline"
      exports: ["HeroCard"]
    - path: "src/components/grants/match-card.tsx"
      provides: "Scored grant card with action buttons"
      exports: ["MatchCard"]
    - path: "src/components/grants/profile-sidebar.tsx"
      provides: "Company profile sidebar/collapsible panel"
      exports: ["ProfileSidebar"]
    - path: "src/components/grants/match-list.tsx"
      provides: "Ranked list combining hero + match cards"
      exports: ["MatchList"]
    - path: "src/components/grants/results-layout.tsx"
      provides: "Responsive layout with sidebar desktop / collapsible mobile"
      exports: ["ResultsLayout"]
  key_links:
    - from: "src/components/grants/match-list.tsx"
      to: "src/components/grants/hero-card.tsx"
      via: "renders HeroCard for scores[0]"
      pattern: "HeroCard"
    - from: "src/components/grants/match-list.tsx"
      to: "src/components/grants/match-card.tsx"
      via: "renders MatchCard for remaining scores"
      pattern: "MatchCard"
    - from: "src/components/grants/results-layout.tsx"
      to: "src/components/grants/profile-sidebar.tsx"
      via: "renders ProfileSidebar in desktop aside and mobile details"
      pattern: "ProfileSidebar"
---

<objective>
Build the 7 UI components for the grant matching results display: score badge, hero card (top match), match card (other matches), improvement tips, match list, profile sidebar, and responsive results layout. All components are tested with jsdom unit tests for the three key components (hero-card, match-card, profile-sidebar) per VALIDATION.md Wave 0 requirements.

Purpose: These components are the visual layer of Phase 3 -- what users see when they get their personalized grant recommendations.
Output: 7 UI components, 3 component test files, all tests green.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-grant-matching/03-RESEARCH.md
@.planning/phases/03-grant-matching/03-01-SUMMARY.md

<interfaces>
<!-- Types created by Plan 01 that this plan consumes -->

From src/lib/matching/types.ts (created by 03-01):
```typescript
export interface EligibilityRule {
  rule: string
  field: string
  operator: string
  value: unknown
}

export interface GrantWithRules {
  id: string
  name: string
  provider_agency: string
  description: string | null
  max_funding: number | null
  currency: string
  deadline: string | null
  eligibility_rules: EligibilityRule[] | null
  scoring_rubric: unknown
}

export interface GrantScore {
  grant_id: string
  score: number
  explanation: string
  improvement_suggestions?: string[]
}

export interface MatchResult {
  profile: Record<string, unknown>
  scores: GrantScore[]
  grants: GrantWithRules[]
  totalGrants: number
  filteredCount: number
}
```

From src/app/grants/browse/grant-card.tsx (existing Grant interface -- DO NOT use this for match results; use GrantWithRules instead since it includes eligibility_rules and scoring_rubric):
```typescript
export interface Grant {
  id: string
  name: string
  provider_agency: string
  description: string | null
  max_funding: number | null
  currency: string
  deadline: string | null
}
```

From src/app/(home)/profile-result.tsx (reuse FieldRow/ListField patterns):
```typescript
function FieldRow({ label, value }: { label: string; value?: string | null })
function ListField({ label, items }: { label: string; items?: string[] })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UI components with component tests for hero-card, match-card, and profile-sidebar</name>
  <files>src/components/grants/score-badge.tsx, src/components/grants/hero-card.tsx, src/components/grants/match-card.tsx, src/components/grants/improvement-tips.tsx, src/components/grants/match-list.tsx, src/components/grants/profile-sidebar.tsx, src/components/grants/results-layout.tsx, src/components/grants/__tests__/hero-card.test.ts, src/components/grants/__tests__/match-card.test.ts, src/components/grants/__tests__/profile-sidebar.test.ts</files>
  <behavior>
    hero-card tests (jsdom):
    - Test: renders grant name as heading
    - Test: renders provider_agency in a badge
    - Test: renders ScoreBadge with the correct score percentage
    - Test: renders AI explanation text from score.explanation
    - Test: renders formatted funding amount and currency
    - Test: renders formatted deadline date
    - Test: renders "Aplica acum" link pointing to /grants/{grant.id}
    - Test: renders disabled "Salveaza" button

    match-card tests (jsdom):
    - Test: renders grant name and score badge
    - Test: renders "Aplica acum" link and disabled "Salveaza" button
    - Test: renders ImprovementTips when score < 50 and improvement_suggestions exist
    - Test: does NOT render ImprovementTips when score >= 50

    profile-sidebar tests (jsdom):
    - Test: renders "Profilul companiei" as card title
    - Test: renders company_name, industry, location, legal_form field rows
    - Test: renders purchase_need when present in profile
    - Test: renders "Editeaza profilul" link pointing to /
  </behavior>
  <action>
    **RED phase -- write all 3 test files first:**

    1. Create `src/components/grants/__tests__/hero-card.test.ts` with `// @vitest-environment jsdom` annotation. Import `render`, `screen` from `@testing-library/react`. Create mock GrantWithRules and GrantScore fixtures. Write all 8 hero-card tests from behavior block. Run -- must FAIL (RED).

    2. Create `src/components/grants/__tests__/match-card.test.ts` with jsdom annotation. Write all 4 match-card tests. Run -- must FAIL (RED).

    3. Create `src/components/grants/__tests__/profile-sidebar.test.ts` with jsdom annotation. Write all 4 profile-sidebar tests. Create a mock profile object with company_name, industry, location, legal_form, purchase_need fields. Run -- must FAIL (RED).

    **GREEN phase -- create all 7 components:**

    4. **score-badge.tsx** -- Export `ScoreBadge({ score }: { score: number })`. Three color tiers:
       - score >= 75: green (bg-green-100 text-green-800 border-green-200)
       - score >= 50: yellow (bg-yellow-100 text-yellow-800 border-yellow-200)
       - score < 50: red (bg-red-100 text-red-800 border-red-200)
       - Render: `<span>` with rounded-full border, shows `{score}%`

    5. **hero-card.tsx** -- Export `HeroCard({ grant, score }: { grant: GrantWithRules, score: GrantScore })` (MATCH-03). A prominent card for the #1 match:
       - ScoreBadge large in top-right
       - Grant name as CardTitle (text-xl)
       - Badge with provider_agency
       - AI explanation paragraph (`score.explanation`)
       - Two info rows: funding amount (formatted with `Intl.NumberFormat('ro-MD')` + currency) and deadline (formatted with `Intl.DateTimeFormat('ro-MD')`)
       - Two action buttons at bottom: "Aplica acum" (primary, links to `/grants/${grant.id}`) and "Salveaza" (outline, disabled with title="Disponibil dupa autentificare" -- MATCH-05 placeholder until Phase 4)
       - Use `import Link from 'next/link'` for Aplica acum
       - Props use `GrantWithRules` (not `Grant`) since MatchResult returns GrantWithRules[]

    6. **improvement-tips.tsx** -- Export `ImprovementTips({ suggestions }: { suggestions: string[] })`. Renders only if suggestions is non-empty. Yellow/amber info box with lightbulb icon (lucide `Lightbulb`), header "Ce poti imbunatati:" and bulleted list of suggestions.

    7. **match-card.tsx** -- Export `MatchCard({ grant, score }: { grant: GrantWithRules, score: GrantScore })` (MATCH-05). Compact card for non-hero matches:
       - ScoreBadge in header alongside grant name
       - Provider badge, funding, deadline in a compact row
       - AI explanation (line-clamp-3)
       - If score.score < 50 and score.improvement_suggestions exists, render `<ImprovementTips>` (MATCH-04)
       - Two buttons: "Aplica acum" (primary Link) and "Salveaza" (outline, disabled, same placeholder as hero)

    8. **match-list.tsx** -- Export `MatchList({ scores, grants }: { scores: GrantScore[], grants: GrantWithRules[] })`.
       - Build a lookup map: `const grantMap = new Map(grants.map(g => [g.id, g]))` to resolve grant_id references from scores to full grant objects.
       - For scores[0], find grant via `grantMap.get(scores[0].grant_id)` and render `<HeroCard grant={grant} score={scores[0]} />`.
       - For remaining scores, look up each grant and render `<MatchCard>`.
       - If no scores, shows empty state: "Nu am gasit granturi potrivite pentru profilul tau." with a Link to `/grants/browse` labeled "Exploreaza toate granturile".
       - Skip any score whose grant_id does not resolve in the map (defensive).

    9. **profile-sidebar.tsx** -- Export `ProfileSidebar({ profile }: { profile: Record<string, unknown> })` (MATCH-06). Displays company data in a Card:
       - CardTitle: "Profilul companiei"
       - FieldRow-style rows for: company_name ("Denumire"), industry ("Activitate"), location ("Locatie"), legal_form ("Forma juridica")
       - If `profile.purchase_need` exists, show it as a row
       - Edit link at bottom: `<Link href="/">Editeaza profilul</Link>` with Pencil icon (lucide)

    10. **results-layout.tsx** -- Export `ResultsLayout({ profile, shareToken, children })` (MATCH-06 responsive layout):
        - Mobile (`md:hidden`): `<details>` element with `<summary>Profilul companiei tale</summary>` wrapping `<ProfileSidebar>`
        - Desktop (`hidden md:block md:w-80 md:shrink-0`): sticky `<aside>` with `<ProfileSidebar>`
        - Main content area: `<main className="min-w-0 flex-1">{children}</main>`
        - Outer wrapper: `max-w-6xl mx-auto px-4 py-8 flex gap-8`
        - Header above layout: page title "Rezultatele tale" (h1) and a Share button (lucide `Share2` icon) if `shareToken` prop is provided. Share button copies `${window.location.origin}/results/${shareToken}` to clipboard using `navigator.clipboard.writeText`, shows "Copiat!" toast via a simple state toggle (no external toast library needed -- use a temporary span).

    11. Run all 3 component test files -- they must PASS (GREEN).

    12. Run full test suite to confirm no regressions: `npx vitest run`
  </action>
  <verify>
    <automated>npx vitest run src/components/grants/__tests__/hero-card.test.ts src/components/grants/__tests__/match-card.test.ts src/components/grants/__tests__/profile-sidebar.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 7 components created. HeroCard renders grant name, provider badge, score, funding, deadline, explanation, and two action buttons -- 8 tests green. MatchCard renders compact card with ImprovementTips for sub-50 scores -- 4 tests green. ProfileSidebar renders company fields and edit link -- 4 tests green. Full test suite passes with no regressions.</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
All component tests pass (hero-card: 8, match-card: 4, profile-sidebar: 4). No regressions in existing Phase 1-2 tests or Plan 01 matching tests.
</verification>

<success_criteria>
- Hero card displays grant name, provider, score badge, funding, deadline, AI explanation, and two action buttons (MATCH-03, MATCH-05)
- Match cards show scored grants with improvement tips for sub-50% matches (MATCH-04)
- Profile sidebar visible with company data and edit link (MATCH-06)
- MatchList correctly maps GrantScore.grant_id to GrantWithRules via lookup map
- All 16 component tests pass
- Full test suite green with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-grant-matching/03-02-SUMMARY.md`
</output>
