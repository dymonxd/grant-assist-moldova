---
phase: 03-grant-matching
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false
requirements:
  - MATCH-01
  - MATCH-02
  - MATCH-03
  - MATCH-04
  - MATCH-05
  - MATCH-06
  - MATCH-07
  - MATCH-08

must_haves:
  truths:
    - "User completes profile flow and sees ranked grant matches with scores and explanations"
    - "Top match is visually prominent as a hero card"
    - "Sub-50% matches show improvement suggestions"
    - "Profile sidebar visible on desktop, collapsible on mobile"
    - "Share link works in a separate browser context without authentication"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 3 grant matching flow end-to-end: profile creation through to results display, share link functionality, and responsive layout. This is the final verification before marking Phase 3 complete.

Purpose: Confirm all MATCH requirements work together as a cohesive user experience.
Output: Verified Phase 3 functionality, ready for Phase 4 or Phase 5.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-grant-matching/03-01-SUMMARY.md
@.planning/phases/03-grant-matching/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and dev server smoke check</name>
  <files></files>
  <action>
    1. Run full test suite: `npx vitest run --reporter=verbose`
    2. Start dev server: `npx next dev`
    3. Verify the dev server compiles without errors (check terminal output for build errors)
    4. If any tests fail or build errors exist, fix them before proceeding to the checkpoint
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All tests pass. Dev server compiles cleanly. No type errors or import resolution failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 3 grant matching flow</name>
  <files></files>
  <action>
    What was built:
    - Two-stage matching engine (SQL pre-filter + AI ranking)
    - Results page with hero card, scored grant list, profile sidebar
    - Shareable results links with 30-day expiry
    - Landing flow redirects to /results after profile creation

    Verification steps for the user:
    1. Open http://localhost:3000 in your browser
    2. Enter an IDNO (try 1003600070656) or describe a business idea
    3. Confirm profile loads, then select a purchase need
    4. After saving purchase need, verify you are redirected to /results (not the old static complete page)
    5. On /results, verify:
       a. Top match appears as a prominent hero card with score badge, grant name, provider, funding, deadline, and AI explanation paragraph
       b. Other matches appear below as compact cards with score badges
       c. Any match below 50% shows yellow improvement suggestions
       d. Each card has "Aplica acum" (primary) and "Salveaza" (disabled/outline) buttons
       e. Summary text shows "Am gasit X granturi potrivite din Y disponibile"
    6. Desktop (widen browser to 1280px+): verify profile sidebar is visible on the left with company data and "Editeaza profilul" link
    7. Mobile (narrow browser to 375px or use DevTools responsive mode): verify sidebar is hidden and replaced by a collapsible "Profilul companiei tale" details element -- tap to expand
    8. Click the Share button (top of page) -- verify a link is copied to clipboard
    9. Open the copied link in an incognito/private window -- verify results page loads without authentication, shows "Aceasta pagina a fost partajata cu tine" banner, and displays matching grants
    10. Try visiting /results/invalid-token -- verify the not-found page shows "Linkul de partajare este invalid sau a expirat"

    Resume signal: Type "approved" to mark Phase 3 complete, or describe any issues to fix.
  </action>
  <verify>Human visual and functional verification of the complete grant matching flow</verify>
  <done>User approves the end-to-end Phase 3 experience. All MATCH requirements verified working.</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
Full test suite green. Human verification of end-to-end flow confirms all MATCH requirements.
</verification>

<success_criteria>
- Full test suite passes
- User can complete the profile flow and see ranked grant matches
- Hero card, match cards, improvement tips, profile sidebar all render correctly
- Share link works in a separate browser without auth
- Responsive layout works on desktop and mobile
- Human approves the end-to-end experience
</success_criteria>

<output>
After completion, create `.planning/phases/03-grant-matching/03-03-SUMMARY.md`
</output>
