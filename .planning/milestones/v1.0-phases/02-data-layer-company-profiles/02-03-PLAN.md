---
phase: 02-data-layer-company-profiles
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/(home)/page.tsx
  - src/app/(home)/idno-form.tsx
  - src/app/(home)/idea-form.tsx
  - src/app/(home)/profile-result.tsx
  - src/app/(home)/manual-form.tsx
  - src/app/(home)/purchase-chips.tsx
  - src/app/(home)/partial-badge.tsx
  - src/app/(home)/__tests__/purchase-chips.test.ts
  - src/app/page.tsx
autonomous: false

requirements: [PROF-01, PROF-02, PROF-04, PROF-06, PURCH-01, PURCH-02, PURCH-03]

must_haves:
  truths:
    - "User can enter a 13-digit IDNO and see auto-populated company data within seconds, with loading feedback during scraping"
    - "User can describe a business idea in free text and see an AI-inferred company profile"
    - "User who gets zero data from all registries sees a manual entry form with company name, industry, location, legal form fields"
    - "Date partiale badge appears when some sources succeed but others fail"
    - "User can select a purchase need via quick-select chips (4 visible, expandable to 8) or type custom text, with chip click pre-filling the textarea"
    - "All UI text is in Romanian, mobile-responsive at 375px, with loading and error states"
    - "Purchase chips component has unit tests verifying chip pre-fill behavior"
  artifacts:
    - path: "src/app/(home)/page.tsx"
      provides: "Landing page orchestrating the profile creation flow"
      min_lines: 40
    - path: "src/app/(home)/idno-form.tsx"
      provides: "Client component: IDNO input with validation and submit"
      contains: "useActionState"
    - path: "src/app/(home)/idea-form.tsx"
      provides: "Client component: business idea textarea with submit"
      contains: "useActionState"
    - path: "src/app/(home)/profile-result.tsx"
      provides: "Client component: displays populated company profile data"
    - path: "src/app/(home)/manual-form.tsx"
      provides: "Client component: manual entry fallback form"
    - path: "src/app/(home)/purchase-chips.tsx"
      provides: "Client component: chip selection with text field pre-fill"
    - path: "src/app/(home)/partial-badge.tsx"
      provides: "Date partiale indicator badge"
    - path: "src/app/(home)/__tests__/purchase-chips.test.ts"
      provides: "Unit tests for chip pre-fill and expand behavior"
  key_links:
    - from: "src/app/(home)/idno-form.tsx"
      to: "src/app/actions/profile.ts"
      via: "lookupCompany server action call"
      pattern: "lookupCompany"
    - from: "src/app/(home)/idea-form.tsx"
      to: "src/app/actions/profile.ts"
      via: "inferFromIdea server action call"
      pattern: "inferFromIdea"
    - from: "src/app/(home)/manual-form.tsx"
      to: "src/app/actions/profile.ts"
      via: "saveManualProfile server action call"
      pattern: "saveManualProfile"
    - from: "src/app/(home)/purchase-chips.tsx"
      to: "src/app/actions/purchase.ts"
      via: "savePurchaseNeed server action call"
      pattern: "savePurchaseNeed"
---

<objective>
Replace the placeholder landing page with the full company profile creation flow: IDNO lookup form, business idea form, auto-populated profile result display, manual entry fallback, partial data badge, and purchase need selection with chip UI. This is the "magic moment" page where users see their company data appear automatically. Includes unit tests for purchase chips behavioral requirements.

Purpose: This is the primary user-facing deliverable of Phase 2 -- the entry point where entrepreneurs interact with the platform for the first time. It must feel fast, handle all edge cases (partial data, all-fail, AI inference), and guide users through to purchase need selection.

Output: Working landing page with complete profile creation flow ready for Phase 3 matching.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer-company-profiles/02-RESEARCH.md
@.planning/phases/02-data-layer-company-profiles/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 02-02 Server Actions -->

From src/app/actions/profile.ts:
```typescript
'use server'
export async function lookupCompany(idno: string): Promise<{
  profile?: CompanyProfileRow
  sourceStatus?: Record<string, 'success' | 'error' | 'timeout'>
  isPartial?: boolean
  allFailed?: boolean
  error?: string
}>
export async function inferFromIdea(businessIdea: string): Promise<{
  profile?: CompanyProfileRow
  error?: string
}>
export async function saveManualProfile(fields: {
  company_name: string
  industry: string
  location: string
  legal_form: string
}): Promise<{ profile?: CompanyProfileRow; error?: string }>
```

From src/app/actions/purchase.ts:
```typescript
'use server'
export async function savePurchaseNeed(purchaseNeed: string): Promise<{
  success?: boolean
  error?: string
}>
```

<!-- From Phase 1 UI patterns -->

From src/app/globals.css (theme):
- Brand accent: `--brand: oklch(0.7 0.15 60)` (warm orange)
- Uses shadcn/ui theme variables (primary, muted, accent, etc.)
- Mobile-first: base padding px-4, max-w-5xl container

From src/app/layout.tsx:
- html lang="ro", Geist Sans/Mono fonts
- Header with "GrantAssist Moldova", main content area, footer

Existing shadcn/ui components: button.tsx
Need to install: input, textarea, card, badge (via `npx shadcn@latest add input textarea card badge`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build IDNO form, idea form, profile result, manual form, and partial badge components</name>
  <files>src/app/(home)/idno-form.tsx, src/app/(home)/idea-form.tsx, src/app/(home)/profile-result.tsx, src/app/(home)/manual-form.tsx, src/app/(home)/partial-badge.tsx</files>
  <action>
    First install needed shadcn/ui components: `npx shadcn@latest add input textarea card badge`

    1. Create `src/app/(home)/idno-form.tsx` ('use client'):
       - Accept props: `onResult: (result) => void` callback for when profile data is received.
       - Use React 19 `useActionState` (NOT deprecated useFormState) with the lookupCompany server action.
       - Input field: 13-digit IDNO with placeholder "Introdu IDNO-ul (13 cifre)", maxLength=13, inputMode="numeric", pattern="[0-9]*".
       - Submit button: "Cauta compania" with loading state showing "Se cauta datele companiei..." spinner during server action execution. Use `useTransition` or the pending state from useActionState.
       - Client-side pre-validation: show inline error if input is not 13 digits before submitting.
       - On successful result, call onResult with the profile data.
       - On error, show Romanian error message below the form.
       - Mobile-responsive: full-width input and button.

    2. Create `src/app/(home)/idea-form.tsx` ('use client'):
       - Accept props: `onResult: (result) => void`.
       - Textarea with placeholder "Descrie ideea ta de afacere in cateva propozitii...", min 10 chars.
       - Submit button: "Analizeaza ideea" with loading state "Se analizeaza ideea ta..." (AI inference takes a few seconds).
       - On result, call onResult.
       - On error, show Romanian error message.

    3. Create `src/app/(home)/profile-result.tsx` ('use client'):
       - Accept props: `profile` (company data), `isPartial`, `sourceStatus`, `allFailed`, `onManualEntry: () => void`.
       - Display a shadcn Card with company data: company_name, industry, location, legal_form in a clean grid layout.
       - Show `<PartialBadge />` if isPartial is true.
       - If allFailed is true, show a message "Nu am gasit date pentru acest IDNO" and a button "Completeaza manual" that calls onManualEntry.
       - Each field that is null shows a dash or "Necunoscut" placeholder.
       - Include an "Editeaza" link/button for future use (can be a placeholder for now).

    4. Create `src/app/(home)/manual-form.tsx` ('use client'):
       - Accept props: `onResult: (result) => void`.
       - Four fields: company_name (required), industry (required), location (default "Moldova"), legal_form (select: SRL, II, SA, GTC).
       - Use shadcn Input for text fields. For legal_form, use a simple select element or shadcn Select if installed.
       - Submit button: "Salveaza datele companiei".
       - Calls saveManualProfile server action.
       - On success, calls onResult.

    5. Create `src/app/(home)/partial-badge.tsx`:
       - Accept props: `sourceStatus: Record<string, 'success' | 'error' | 'timeout'>`.
       - Render a shadcn Badge variant="secondary" with text "Date partiale" and an info icon from lucide-react.
       - On hover/click, show a tooltip or expandable section listing which sources succeeded/failed/timed out (e.g., "idno.md: succes, srl.md: eroare, openmoney.md: timeout").
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All five client components created with proper 'use client' directives, server action imports, loading states, error handling, and Romanian text. TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build purchase chips component with unit tests and assemble the landing page</name>
  <files>src/app/(home)/purchase-chips.tsx, src/app/(home)/__tests__/purchase-chips.test.ts, src/app/(home)/page.tsx, src/app/page.tsx</files>
  <behavior>
    - Initially renders exactly 4 chips plus a "Mai multe..." button
    - After clicking "Mai multe...", renders all 8 chips and "Mai multe..." disappears
    - Clicking a chip sets the textarea value to that chip's text (pre-fill behavior per PURCH-03)
    - Clicking a different chip replaces the textarea value with the new chip's text
    - User can type custom text in the textarea independently of chip selection
    - Textarea has Romanian placeholder "Descrie ce doresti sa achizitionezi..."
  </behavior>
  <action>
    1. Create `src/app/(home)/__tests__/purchase-chips.test.ts` FIRST (RED phase):
       - Install `@testing-library/react` and `@testing-library/jest-dom` as dev dependencies if not already present: `npm install -D @testing-library/react @testing-library/jest-dom jsdom`
       - Add `environment: 'jsdom'` to vitest.config.ts test section if not already set (needed for DOM testing). Alternatively, add `// @vitest-environment jsdom` at top of this test file.
       - Mock the `savePurchaseNeed` server action import using `vi.mock('@/app/actions/purchase', ...)`
       - Test cases:
         a. "renders 4 chips initially" -- render PurchaseChips, assert 4 chip buttons visible plus "Mai multe..." button, assert remaining 4 chips NOT in document.
         b. "expands to show all 8 chips on Mai multe click" -- click "Mai multe...", assert all 8 chips visible, "Mai multe..." gone.
         c. "clicking chip pre-fills textarea" -- click first chip ("Echipament si utilaje"), assert textarea value equals "Echipament si utilaje".
         d. "clicking different chip replaces textarea value" -- click first chip, then click second chip ("Software si digitalizare"), assert textarea value equals "Software si digitalizare".
         e. "textarea accepts custom text" -- type "Custom need" in textarea, assert textarea value is "Custom need".

    2. Create `src/app/(home)/purchase-chips.tsx` ('use client') (GREEN phase):
       - Accept props: `onSaved: () => void` callback.
       - Define QUICK_NEEDS array: ['Echipament si utilaje', 'Software si digitalizare', 'Instruirea personalului', 'Materii prime', 'Renovare spatii', 'Transport si logistica', 'Marketing si promovare', 'Consultanta'].
       - Show first 4 chips by default. "Mai multe..." button expands to show all 8.
       - Chips are styled as rounded-full buttons with border (see research code example pattern). Active/selected chip gets a distinct style (bg-primary text-primary-foreground).
       - Clicking a chip sets the textarea value to that chip's text (pre-fill behavior per PURCH-03).
       - Textarea below chips: placeholder "Descrie ce doresti sa achizitionezi..." for custom input or modification of chip text.
       - "Continua" submit button calls savePurchaseNeed server action with the current text value.
       - Loading state during save. On success, calls onSaved.

    3. Create `src/app/(home)/page.tsx` (Server Component wrapper with Client Component orchestrator):
       - This page manages the multi-step flow state: 'input' -> 'profile' -> 'purchase' -> 'complete'.
       - The page itself can be a Server Component that renders a Client Component orchestrator.
       - Create the client orchestrator inline or as a separate component. It manages state:
         - `step`: 'input' | 'profile' | 'purchase' | 'complete'
         - `inputMode`: 'idno' | 'idea' (toggle between the two input modes)
         - `profile`: the company profile data once received
         - `isPartial`, `sourceStatus`, `allFailed`: from lookupCompany result
       - Step 'input': Show a toggle/tabs between "Am IDNO" and "Am o idee de afacere". Below the toggle, render either IdnoForm or IdeaForm. Both have onResult callbacks that set profile state and advance to 'profile' step.
       - Step 'profile': Show ProfileResult component. If allFailed, show manual form. If profile data exists, show a "Continua" button to advance to 'purchase' step.
       - Step 'purchase': Show PurchaseChips component. On saved, advance to 'complete'.
       - Step 'complete': Show a success message "Profilul tau a fost creat!" and a CTA button "Cauta granturi potrivite" linking to /grants/browse (or a future matching page).
       - Heading: "Descopera granturile disponibile pentru afacerea ta" (keep from current page).
       - Subheading: "Introdu IDNO-ul companiei sau descrie ideea ta de afacere".
       - Mobile-responsive: centered layout, max-w-lg, px-4 padding.

    4. Update `src/app/page.tsx`: Since the landing page is now at `src/app/(home)/page.tsx` in a route group, either:
       - Move the new page to `src/app/(home)/page.tsx` and ensure the route group renders at `/` (route groups don't add path segments), OR
       - Replace `src/app/page.tsx` directly with the new content (simpler approach -- no route group needed if the page is self-contained).
       - Choose the simpler approach: replace `src/app/page.tsx` with the new landing page content directly. Use `src/app/` directory for the component files (idno-form.tsx, etc.) as siblings to page.tsx. If the (home) route group adds complexity without benefit, skip it and put components directly in src/app/ or in a dedicated src/app/_components/ directory. The key constraint: components must be co-located with the page that uses them for clarity.
       - DECISION: Put all landing page client components in `src/app/(home)/` route group since the research recommends this structure and it keeps landing page components separate from other routes. The route group `(home)` renders at `/` without adding a URL segment.
  </action>
  <verify>
    <automated>npx vitest run src/app/(home)/__tests__/purchase-chips.test.ts --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>Purchase chips component renders 4 chips (expandable to 8), chip click pre-fills textarea, all 5 tests pass. Landing page assembled with step flow, IDNO/idea toggle, profile display, and purchase chips.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete landing page profile creation flow</name>
  <files>none</files>
  <action>
    Human verifies the complete company profile creation flow built in Tasks 1-2.

    What was built: Complete company profile creation flow on the landing page -- IDNO lookup with auto-population from three Moldovan registries, business idea AI inference, manual entry fallback when all sources fail, "Date partiale" indicator for partial data, and purchase need selection with chip UI.

    How to verify:
    1. Run `npm run dev` and visit http://localhost:3000
    2. Verify the landing page shows a toggle between "Am IDNO" and "Am o idee de afacere"
    3. IDNO flow: Enter a valid 13-digit IDNO (e.g., 1003600070650). Verify:
       - Loading indicator appears ("Se cauta datele companiei...")
       - After scraping completes, company data card appears (some fields may be null if sites are unreachable -- that is expected)
       - If all sources fail, manual entry form appears
       - If partial data, "Date partiale" badge appears
    4. Idea flow: Switch to "Am o idee de afacere", type a business description (e.g., "Vreau sa deschid o cafenea in Chisinau"). Verify:
       - Loading indicator appears
       - AI-inferred profile appears (industry, location, etc.)
    5. Purchase need: After profile appears, advance to purchase step. Verify:
       - 4 chips visible, "Mai multe..." expands to 8
       - Clicking a chip fills the textarea
       - "Continua" button saves and shows completion message
    6. Mobile: Resize browser to 375px width, verify layout remains usable
    7. Check the browser console for any errors
  </action>
  <verify>Human confirms the flow works end-to-end</verify>
  <done>User approves the landing page profile creation flow or provides specific issues to fix</done>
</task>

</tasks>

<verification>
- `npx vitest run src/app/(home)/__tests__/ --reporter=verbose` -- purchase chips tests pass
- `npx tsc --noEmit` -- zero TypeScript errors
- `npm run build` -- Next.js build succeeds
- Landing page renders at / with all components
- IDNO form submits and shows results (or graceful errors)
- Idea form submits and shows AI inference results
- Manual form appears when all sources fail
- Purchase chips work with pre-fill behavior
- Mobile responsive at 375px
</verification>

<success_criteria>
- User enters IDNO and sees company data (or manual fallback if sources fail)
- User describes business idea and sees AI-inferred profile
- "Date partiale" badge shows when some but not all sources succeed
- Purchase need chips pre-fill textarea, custom text accepted
- All UI in Romanian, loading/error states present, mobile-responsive
- Step flow works: input -> profile -> purchase -> complete
- Purchase chips unit tests pass (5 tests covering chip count, expand, pre-fill)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer-company-profiles/02-03-SUMMARY.md`
</output>
