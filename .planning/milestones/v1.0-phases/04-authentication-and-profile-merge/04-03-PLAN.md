---
phase: 04-authentication-and-profile-merge
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/privacy/page.tsx
  - src/app/privacy/__tests__/page.test.ts
autonomous: true
requirements: [GEN-04]

must_haves:
  truths:
    - "User can access /privacy and see a privacy policy page in Romanian"
    - "The page renders with proper heading, sections, and contact information"
  artifacts:
    - path: "src/app/privacy/page.tsx"
      provides: "Static privacy policy page in Romanian"
      min_lines: 30
      contains: "Politica de Confidentialitate"
  key_links: []
---

<objective>
Create the static privacy policy page at /privacy in Romanian, covering data collection, usage, third-party sharing, user rights, and contact information.

Purpose: Fulfills GEN-04 regulatory/transparency requirement. This is a fully independent page with no dependencies on other Phase 4 work.

Output: Static page at `src/app/privacy/page.tsx` with Romanian privacy policy content.
</objective>

<execution_context>
@/Users/dymonxd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dymonxd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create privacy policy page at /privacy</name>
  <files>src/app/privacy/page.tsx, src/app/privacy/__tests__/page.test.ts</files>
  <behavior>
    - Test: page renders with "Politica de Confidentialitate" heading
    - Test: page contains section about data collection ("Datele pe care le colectam")
    - Test: page contains section about data usage
    - Test: page contains section about user rights
  </behavior>
  <action>
Create `src/app/privacy/__tests__/page.test.ts` first with basic render tests using `@testing-library/react`. Use `// @vitest-environment jsdom` annotation at the top of the test file (same pattern as Phase 2 UI tests). Import the page component and render it, asserting key headings and sections exist.

Then create `src/app/privacy/page.tsx` as a server component (no `'use client'` needed -- it is purely static content):

1. Export metadata: `export const metadata = { title: 'Politica de Confidentialitate | GrantAssist Moldova' }`
2. Render a static page with the following sections in Romanian:
   - **Heading:** "Politica de Confidentialitate"
   - **Last updated date** (static)
   - **Section 1 - Datele pe care le colectam:** IDNO, company information from public registries, email, name, phone (at signup), purchase needs, AI-generated content
   - **Section 2 - Cum folosim datele:** Grant matching, AI writing assistance, notifications about deadlines
   - **Section 3 - Partajarea datelor:** We do not sell data. Third parties: Supabase (database/auth), OpenAI (AI processing, anonymized). Public registries queried: openmoney.md, idno.md, srl.md
   - **Section 4 - Stocarea si securitatea:** Data stored in Supabase with RLS, encrypted sessions, HTTPS
   - **Section 5 - Drepturile tale:** Right to access, modify, delete data. Contact instructions.
   - **Section 6 - Cookie-uri:** iron-session encrypted cookie for anonymous profile tracking, Supabase auth cookies
   - **Section 7 - Contact:** Email address for privacy questions
3. Style using Tailwind: `prose` class for readable text layout, `max-w-3xl mx-auto px-4 py-8`
4. All text in Romanian using simple, everyday language (GEN-01 convention)
5. Use semantic HTML: h1, h2, p, ul/li for lists

NOTE: This is a static page. No database queries, no client-side interactivity. Keep it simple.
  </action>
  <verify>
    <automated>npx vitest run src/app/privacy/__tests__/page.test.ts --reporter=verbose</automated>
  </verify>
  <done>Privacy policy page renders at /privacy with Romanian content covering data collection, usage, sharing, security, user rights, cookies, and contact. Test confirms key sections render.</done>
</task>

</tasks>

<verification>
1. Test passes: `npx vitest run src/app/privacy/__tests__/page.test.ts --reporter=verbose`
2. Full suite green: `npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- /privacy page renders with "Politica de Confidentialitate" heading
- All 7 sections present with Romanian content
- Page is static (no client-side JS needed)
- Test confirms key sections render
</success_criteria>

<output>
After completion, create `.planning/phases/04-authentication-and-profile-merge/04-03-SUMMARY.md`
</output>
